# X-Men Legends PS2Recomp TODO

## Immediate Performance Target

- Latest local combined checkpoint (2026-09-06 11:37 UTC): PS2Recomp `49442c6` packs four-channel texture filtering without changing scalar rounding; candidate `23A73821...` also includes outer `c2a4776` bulk VU transfers. Combined audit passed at least 258,049 compiled calls and 567,279,617 filtered samples, zero mismatches/logged guest faults. Uninstrumented first-level run: 128 presents / 21.1616848 seconds = **6.048668 FPS**, all workload gates, exit 0, 1,400-vsync limit. Prior 5.64 FPS observation is a separate shared-host run, not a controlled sustained-speed claim. Test image `63AA8AB3...`: 2,635,008 color cases/four rounding modes, 88/89 GS tests (historical CSR/IMR failure only), unchanged 128-case depth differential, 161 VU tests and ten original replay/budget checks. Benchmark gate tests pass. Full details: `GS-FILTER-CHECKPOINT.md`. All owned tests/builds/games ended, startup restored, no new images or old disc captures. Controls handoff pending; 30 FPS unmet. NEXT: refresh post-change VU/GS cost attribution and target substantial remaining execution/raster overhead; enable the compiled path explicitly in a tested interactive launcher before handoff. Keep all remaining PS2Recomp commits local only, no pushes or PRs. Earlier audit-pending/no-new-game statements below are historical.
- Latest local transfer checkpoint (2026-09-06): whole-game profiling of `A8B0EF85...` verifies 47.59% exclusive VU / 36.82% GS work; CPU raster averages 64.159 ms and 23,423 submissions over presents 1152..1280. Bulk timed payload copies, moved packet ownership and non-throwing expected rejection now pass 372 standalone timing/mutation/wrap/tag cases, all 161 runtime VU tests, ten original replay/budget checks and all four saved failure records. Serial hybrid median costs improve 173.960 -> 162.568 ms / 115.178 -> 108.471 ms (6.5%/5.8%); these are NOT game FPS. Final runtime test image `323E5CEB...`, standalone `C68B5924...`; full evidence in `play-vu-probe/README.md`. No new game executable, image, checkout or build tree. All owned processes closed, startup restored, no old disc images found. NEXT: CPU raster per-pixel texture/write-state overhead, with framebuffer/depth differential validation, then bundle with this VU change for a measured game build. VU-only optimization cannot reach 30 FPS with the present raster cost. Current game baseline remains the prior 5.64/4.77 FPS pair; controls handoff still pending. Keep all commits local, no pushes/PRs.
- Measured local checkpoint (2026-09-06 11:09 UTC): same candidate `A8B0EF85...`, compiled on/off, gives 5.640851 / 4.765298 FPS (128 presents in 22.6916101 / 26.8608598 seconds), about 18.4% improvement in one shared-host pair. Both verified New Game, NYC, native blocks, measurement markers and 1,400-vsync limit; exit 0, zero logged guest faults. Compiled mode remains opt-in and the 30 FPS target is not met. Input disabled, no new visual validation or controls handoff. Both runs ended and restored startup; all owned builds/tests/games are closed. NEXT: profile remaining whole-game VU/GS cost on this audited candidate, pursue substantial performance savings, then explicitly validate input before user handoff. Fixed logs/report slots retain the pair. All checkpoints local only, no pushes or PRs. Earlier baseline and audit-pending statements below are historical.
- Fifth audit completed (2026-09-06 11:02 UTC): candidate `A8B0EF858621D1ECB202E100A4E3B622B3A8B4D5DEA7E7066FD0FD1A7502F043` matched at least 258,049 compiled calls, reached the 1,400-vsync limit, New Game/NYC/both presentation markers, and exited 0 with no logged guest faults. The prior null dispatch was not reproduced in this bounded run; unrestricted play/input remains unverified. Audit runs deliberately produce no FPS and the harness ends with its non-benchmark exception. Next measure compiled mode without the audit, then compare the same binary with compiled mode off. Accelerated logs now use fixed `gameplay-compiled-rate` slots without overwriting baseline logs. PS2Recomp capture checkpoint `09d1cf1` is local only. This supersedes older NEXT/audit-pending entries below.
- Latest corrections (2026-09-06): native-call state optimization, Q waits, immediate normalization and FTOI saturation are fixed locally. Two direct CodeGen regressions fail before/pass after; 32/64/128-bit reloads and unchanged CodeGenTestSuite pass. Runtime image `0A68DF00...` passes 161 default VU tests, all 14 producer integration cases, ten original replay/budget checks and all four saved failure records (EFU fallback; the other three compiled). Q-call recording is now an exact 2,874-cycle/13-packet match; FTOI exact 263 cycles/three packets, with 192 public conversion cases. Details and full identities: `play-vu-probe/README.md`. CodeGen patch is mandatory in CMake; all patches reverse-check cleanly. No push/PR.
- Fourth game audit, candidate `A25C7698...`, stopped after 5,991 accepted calls at tick 381 / PC `0x16a8`; it found the now-fixed FTOI positive-overflow conversion. This was not a speed benchmark. Next candidate with the complete corrections is awaiting audit. Keep compiled mode off by default and do not claim the original guest null-object failure or practical playability resolved. Current GS result is 86/87 with the historical CSR/IMR failure.
- Capture cleanup is now implemented in the performance build: no unconditional first-nonblack scan/dump or preset-frame image series. Explicit range/latest/first-nonblack capture remains opt-in. No disc images older than 12 hours were found; reused one build tree/log slots and saved only two additional small private regression recordings. Protected artifacts and unrelated edits remain untouched. The earlier screenshot-cleanup TODO below is superseded.
- Latest local audit checkpoint (2026-09-06 10:35 UTC): compiled execution remains opt-in/off by default. A pre-publication shadow audit now compares complete drained architecture, memory and timed graphics against the original engine, saving only the first failing replay. Benchmark audit runs never report FPS, disable input, stop their owned process on mismatch and restore startup. All remaining PS2Recomp commits stay local; no push or PR.
- Fixed incoming VF hazard waits aging pending writes incorrectly: the captured PC `0x528` workload now matches all registers, memory, packets and 276 cycles instead of 278. Seventy-two register/read-order/gap regressions pass. Reached EFU arithmetic blocks instead conservatively fall back, discarding private staged writes/packets and caching rejection only for that entry and exact code. This guards a separate PC `0x22b0` mismatch; EFU arithmetic/timing itself is not fixed.
- Current test SHA `CF73728C2E12926AC13E3140B945BA592C02F7289833353D772081C37A09583C`: 156/156 default VU tests, nine compiled integration tests, both original captures at normal/1/8/16/64-cycle budgets pass. Standalone `831AC8DB...` passes the 21 upstream tests and the captured incoming-wait workload. Updated Play source patch reverse-applies cleanly; patch files retain LF on checkout.
- NEXT: candidate `9CC0649BC344544A6A39F0DF85E6DF65836F6A25C2EBE400BC9DC31909842B44` passed 7,148 audited compiled calls before tick 469 / PC `0x540` / cycle 37118928. New Game and NYC package markers appeared, but the audit stopped before mismatching live publication. Private `disc/vu-compiled-failure.bin` replays exactly on the original engine (digest `ddf5b50851320b9f`); compiled output differs in Q, 12 VF words, 88 data bytes, packet bytes and 20 cycles (2854 vs 2874). Trace the first divergence in this recording, including Q readiness and static/incoming waits; do not assume either is the cause. Fixed `gameplay-compiled-audit` and `play-vu-failure.log` retain diagnostics. Older small EFU/entry-wait records are preserved separately. No valid new FPS, no interactive handoff; last valid baseline remains 4.97750 FPS. All owned test/build/game processes ended. This supersedes the historical next steps below.
- Current experimental checkpoint (2026-09-06): opt-in compiled VU runtime hook and hybrid replay checks are integrated, off by default. Final test image `86FC5144...` passes 153 default-mode VU tests, six real producer tests, both original raw replay sets at normal/1/8/16/64 cycles, and hybrid full/64-cycle checks. Actual warm hybrid workload median ratios are 2.257x/1.418x, not gameplay FPS. One older globally-enabled test still rejects inactive serialized bookkeeping; retained unchanged and supplemented with explicit mixed-packet architectural checks. Details and exact identities: `play-vu-probe/README.md`.
- Candidate `AAAC937D...` includes the opt-in performance experiment and title/FPS counter. Compiled ON: at least 208,897 accepted calls, but guest fault before measurement markers at null virtual dispatch `0x396e7c` / return `0x396e84`; no valid FPS. OFF, same binary: zero guest faults, workload verified, 128 presents / 25.715718 seconds = 4.97750 FPS at 2026-09-06 09:56 UTC. No proven whole-game speedup or control handoff. Preserve fixed compiled-failure logs and baseline `gameplay-rate` evidence. NEXT: locate the first compiled-on divergence; inspect singleton allocation `0x38cfd0` and existing allocator hooks `0x14cb28/0x14cb3c`, without bypassing the null dispatch. Older similar failures do not prove this one unrelated to the new engine. Both tests ended and restored startup. Historical next-step/candidate statements below are superseded.
- Cleanup follow-up: `gs_frontend.cpp` still automatically writes first-nonblack and hardcoded present-index PPMs even without `-CaptureFrame`. Make these opt-in in the next performance build; do not link a standalone screenshot-only game build. Preserve intentional evidence and remove obsolete images regularly.
- Pending cleanup: obsolete `out/xmen-final3-build/ps2xTest/play_vu_runtime_adapter.*`, `play_vu_session_refresh.*` and `Release/play_vu_runtime_adapter.lib` remain after moving these targets into `ps2xRuntime`. Project-reference inspection confirms only the obsolete projects refer to the old paths. A containment-checked PowerShell removal was rejected by execution policy before running; nothing was deleted and no alternate deletion method was attempted.
- User contribution restriction: stop opening PS2Recomp pull requests. Keep all further PS2Recomp commits local only; do not push the fork or upstream unless the user explicitly lifts this restriction. Continue implementation and testing locally.
- Latest local checkpoint: PS2Recomp `29310f3` enables opt-in external producer tests; outer `play-vu-probe/runtime_adapter.cpp` calls the real capture/commit boundary. Test image `EDD4F1D4899AB1D4213541DCF1FF4E1BBDBB705E38F1022C0FB001B6F95D7522` passes 151 VU tests including four compiled producer commit/resume/graphics/rejection cases. Signed VI values survive interpreter continuation; packets reach GS exactly once; pre-commit failure publishes nothing; post-commit graphics exceptions propagate rather than request fallback. Both old 32-record replay sets retain exact digests at normal/1/8/16/64 cycles, still through the original engine. NEXT: opt-in full-drain execution hook, hybrid replay validation, then measure a game build. No new game executable or gameplay FPS result yet. Existing standalone libraries/build are reused, all owned processes ended, no new screenshots or old disc images to clean. No push/PR; nested and outer checkpoints stay local. The detached-only next steps in preceding-history entries below are superseded.
- Current local typed producer `CA7AB282F405BBAA70265C0D0DA59223731E13080231B446054F2D8BD36ACEF0` matches all 22 eligible recordings' architectural values, memory and timed graphics packets. Matched runtime disassembly and a bounded experiment identify host FMA contraction as the old VF/Q/data difference source. Immutable `RuntimeFused` mode now explicitly matches that behavior; default `Separate` and its edge tests remain unchanged. Full flag export (0xfff/0xffff), 4,096 status round trips, 64 pending/reset cases, 39,168 dual-model comparisons and 128 compiled model tests pass. VI is compared at its architectural 16-bit width, with host sign-extension differences reported separately. Three-round eligible timing ratios remain about 3.66x/2.90x; no gameplay FPS claim. NEXT: actual runtime commit/resume/fallback tests, then guarded runtime integration and a measured game build. Do not keep chasing the now-explained recorded rounding differences or add another detached adapter. Preserve fallback for unsupported states and extend exceptional-arithmetic/VI/store coverage. No game build/image/push/PR; all preceding partial-mask/numeric-difference entries are history. Details are in play-vu-probe/README.md.
- Current local image `2FBC117747FA8D516DD5BAA954B26299B05E8C9C45E624553FF06D31ACE07CD2` removes internal AVX2 classifier calls and reduces flag/operand bookkeeping. All 19,584 scalar/vector comparisons and previous regressions pass. Opt-in RSQRT now uses a direct quotient with absolute radicand; 256 tests cover signed finite inputs, components and 13-cycle Q/flag visibility (negative-radicand regression failed before). Three-round complete-call medians improve from 125.527/37.790 ms to 84.514/27.466 ms, about 33%/27% lower, not gameplay FPS. Full MAC/STATUS and completion cycles still match all 22 eligible recordings, but the old Q/vertex/memory differences remain. Keep partial masks and runtime-accepted=0; trace the first differing arithmetic inputs/results and finish exceptional FMAC/VI/store semantics before integration. No game build, screenshot, push or PR. Detailed identities, timing scope and caveats are in play-vu-probe/README.md; following entries are history.
- Current local FMAC range prototype computes Z/S/U/O plus product-sticky masks through selected host arithmetic functions, with scalar and CPUID/OS-gated AVX2 objects in one build. Six explicit range/value/timing tests, 1,152 scalar/AVX2 comparisons and 64 independent flag import/reset/export cases pass with all previous tests. Final image `B7C7B9BDD388C423D7C98246C00BF316DB92A086B42F07C0B9E851389BE1FB72` matches full MAC/STATUS in all 22 eligible recordings. This costs throughput: complete-call medians 127.142986/38.926492 ms versus eligible baseline 310.970/83.733 ms, about 2.45x/2.15x (not the older 5x/4x and not gameplay FPS). Keep export masks 0xcf3/0x00ff and runtime-accepted=0 until exceptional MADD/MSUB product/ACC overflow/cancellation, cross masks and general VI/store/numeric gaps are verified. Next reduce per-FMAC host-call/classification cost while completing those semantics; do not add another adapter or package an unverified build. No game binary, image, checkout, PR or push created. Full evidence and caveats are in play-vu-probe/README.md.
- Latest local Play! checkpoint separates current STATUS from MAC using the high half of the existing sticky pipeline. Same-pair FSSET suppresses the upper STATUS update only; I-bit immediate payloads are excluded. Sixteen paired/I-bit cases and sixteen typed initial/pending/reset/export cases pass, plus all prior tests. Final image `7C43EA3C79BA8C7EAA64187E1203C88AC15BA26AD68363BC1CC7537968433A2E` retains the 22 eligible recordings' supported flags/cycles and repeated typed outputs. Three comparisons retain about 5.33x/4.02x eligible median timing ratios, not gameplay FPS. No additional pipeline, MIPS layout change, per-arithmetic callback, game build, screenshot, push or PR. Next implement FMAC overflow/underflow and resolve remaining numeric/VI/store differences; masks remain 0xcf3/0x00ff and runtime-accepted=0. Manual-based priority evidence and measurements are in play-vu-probe/README.md. The same-pair TODO in the older checkpoint below is now resolved.
- Current local Play! checkpoint fixes premature sign/zero sticky resets by queuing FSSET in the native four-cycle pipeline. Eight ordering cases fail before/pass after; all public tests and 22 eligible recorded workloads pass their existing diagnostic checks on image `1F22FC558565019D3C9E062E39F1A1F233B3C039E44FEB122E560FCC27B53CB7`. No per-arithmetic callback, game build, screenshot, push or FPS claim. Still handle same-pair upper arithmetic/FSSET STATUS suppression independently of MAC, FMAC overflow/underflow, and remaining numeric/VI/store differences; runtime-accepted remains zero. The scalar timing measurements below belong to the preceding image.
- Latest local Play! bridge adds a bounded timed I/D/IS/DS status queue and opt-in compiled status callbacks for DIV/SQRT/RSQRT/FSSET/FSAND/FSOR. It imports sticky state and pending resets, retires flags with Q, orders same-cycle FSSET before FDIV, and includes pending flag tails in elapsed-budget rejection. No callback retains original Play! behavior. The no-callback zero/zero baseline fails the new visibility/value regression; 13 cases and additional ordering/tail tests pass alongside all previous public tests.
- Final local Play! image `E327761FDA2417E359622919BE06F627671CDAC161E905D8A6CA708339DFB646`: all 22 eligible recordings match scalar status (14 original, eight spread), with repeated typed outputs across 256 warm calls. STATUS coverage increases to 0xcf3, MAC remains 0x00ff, runtime-accepted=0. Added flags retain about 5.45x/3.97x eligible median timing ratios in three comparisons; this is not gameplay FPS. Next implement FMAC overflow/underflow and full sticky/reset behavior, then resolve VI/store timing and numeric differences before runtime acceptance. Do not claim Q numerical behavior or full scalar fidelity fixed: PS2Recomp's RSQRT zero/zero result/flags differ from the inspected PCSX2 reference.
- No game executable, screenshot, clone or build variant was created. No disc images are older than 12 hours; all owned compiler/test/game processes are closed. Current checkpoint stays local and no new PR or push was made. The typed-bridge checkpoint below is prior history; its 0xe3 mask is superseded by the new 0xcf3 coverage.
- User handoff requirement: only launch/package the next game build with FPS-improving changes, including the queued title/FPS counter. When launched with host controls enabled, explicitly announce "Controls enabled: ready for you to test", stop automated testing/replacement builds, and await user feedback. Do not confuse input-disabled benchmarks with that handoff; do not restart after the user closes the window. The user requests the goal stall at that point; use the goal tool only when its blocked-status requirements are satisfied.
- PS2Recomp `3060eab` adds a guarded typed state capture/commit boundary, with no execution hook. Test image `77E6FDB365436C8B1E38E218E9DC3A0106FCB0D54CD6ACAB4CB019A8EEE95782` passes 147 VU tests and both recordings at normal/1/8/16/64-cycle budgets with unchanged digests. Canonical VF0 now uses raw bits; stale-state comparison ignores padding. Rejections publish nothing, and a committed result can resume through the existing runtime. Full STATUS/MAC masks remain required.
- New `play_vu_runtime_bridge` consumes typed runtime inputs and produces staged outputs without a replay parser/TestVm. All 22 eligible records match independent import/export diagnostics and repeat across 256 warm complete calls. Public invalid-state/ownership/deadline/mask, immutable-input and recovery tests pass alongside all prior tests on Play! image `DF4A313485CCB15E4DFC23D5B38B2A772E00CE68B250B4E5CA97F5F7622BAC69`. Current masks 0x0e3/0x00ff mean runtime-accepted=0; this is not game integration or PS2Recomp compatibility.
- Three paired comparisons include typed import/export, internal copies, FP restoration and staging: eligible median baseline/bridge 320.304/54.734586 ms original, 84.409/19.481892 ms spread (256 warm repetitions). Runtime capture/commit, GS submission, cold compilation and returned-output disposal are excluded. No hybrid or gameplay FPS claim. Full numbers are in play-vu-probe/README.md. Next resolve incomplete flag semantics and general VI/store timing plus existing arithmetic/state differences, then integrate and measure an actual guarded runtime path; do not add more wrappers around an engine that still cannot commit.
- Game candidate SHA remains `61E24D60BF559B6BA7382E498905D6287171E8F3A5DF738D99CAB029E6D91D09`. All owned compiler/test/game processes are closed. No images or extra checkout/build were created; no disc image is older than 12 hours. Workspace inventory is 8,852,832,744 bytes, one Play! reference/build 76,357,616 bytes. No controls-enabled handoff yet.
- The reusable `play_vu_session` static library uses Play! core directly without TestVm. Calls execute on private code/data/state with buffered graphics, preserve the caller's FP environment/MXCSR, invalidate cached code on changes, and return empty rejected output on unsupported entry or failed/over-budget runs. Budget checks include known VF/flag/scalar/transfer tails. The earlier checkpoints below are history, not newer status.
- Public detached-session tests cover immutable inputs, caller rounding/flags, cache reuse/replacement, short-slice and late-budget rejection, scalar-tail rejection, recovery and 1 MiB packet staging cap/reset. Final image `E7C86144FCAD459DD2E7F626266EBF452DA515FAC1420B19EF08EC1BB1A43C72` passes all public tests; all 22 eligible recordings exactly match the direct compiled probe's raw state/memory/staged packets/times. This does not resolve the probe's existing PS2Recomp differences; runtime-accepted=0 remains explicit.
- Earlier plan to add the typed state adapter is completed above, but no game execution path has been enabled. Handle unknown flags and general VI/store timing before accepting those paths, preserving fallback with no external effects on rejection. Two obsolete probe object files were removed after CMake ownership verification; one checkout/build retained, no new images or game windows.
- Latest isolated completed-state conversion retires scalar/flag queues and accounts for transfer/VF/scalar tails without modifying source state. All 22 eligible records match final drain cycles, supported MAC/STATUS and CLIP; 19 also match Q/P. Original 6/14 and spread 29 retain Q differences of 1/2/1 ULP. STATUS mask is explicitly 0x0e3, MAC mask 0xff; unknown bits are not fabricated from expected data. New synthetic drain test and all prior tests pass on Play! image `16A8B4C85C187B1B9EFB651822A327D1C1110F1651F3E0C6055F6735F9326950`. Full-state accepted=0, no game build.
- Three paired recording comparisons show eligible full-drain cases account for about 80% original / 50% spread baseline VU time. Baseline all-case medians 397.280/169.159 ms, compiled eligible medians 80.262/24.610 ms, 256 warm repeats. Adding unchanged fallback projects about 160/109 ms, but that hybrid has NOT been built or measured and import/export costs are excluded. Captures are not frequency-weighted gameplay profiles. Full numbers and binary identities are in `play-vu-probe/README.md`; do not convert them into a gameplay FPS claim.
- Prioritize a guarded full-drain runtime bridge, retaining the existing engine for unsupported states/short slices, rather than requiring every instruction form before any integration. This is an initial optimization path, not reduced goal scope. Must preserve architectural writeback, caller floating-point environment, flag behavior, active transfer ordering and fallback before side effects. The current control helper does not yet cover general VI/store latency or CPU-visible short-slice export. Keep the 30 FPS gameplay target and controls-enabled user handoff unchanged. All owned tests are closed; no new images, clones or game binaries this turn.
- Latest isolated fix initializes idle Q/P pipeline held values as well as architectural mirrors. A new public MULq/WAITQ/WAITP regression failed before the fix (all zero, exit 8) and passes afterward (0.5/0.5/0.25). This was an importer bug, not evidence of incorrect upstream arithmetic. Play! image `6E00516C8DD628CA03954A621328F11F0B9931B8883F2B820B094585A75CA681` passes all public tests and both diagnostics; 22 eligible records retain exact transfer completion times, accepted=0.
- Cold memory tracing confirms the first zeroed vertex component in spread case 9 is fixed (PC 0x2b88, cycle 7, offset 0x27d0). Case 9 packet-byte differences decrease 78->59, memory-byte differences 176->153; case 29 packet-byte differences 35->16, final memory-byte differences 32. Remaining numeric/state mismatches are not declared harmless. Next prioritize architectural scheduling/import-export rather than treating small float differences as a substitute for gameplay progress.
- Baseline replay helper now supports a bounded cold-only one-cycle memory trace, selected by `PS2X_VU_REPLAY_MEMORY_TRACE_CASE`; it is not a game hot-loop hook. Traced spread still passes exact final comparisons. Baseline image `E399C97DDE0E580E98CCDD9FE97D0B7310C9E30168E7A4BFC4B8B4F30FE8C1C7` passes all 146 VU tests and all ten capture/budget checks, unchanged digests. Two small ignored text logs reuse fixed slots; no screenshots, new clone, or game executable was made. All owned build/test processes are terminal. Controls-enabled handoff remains pending a measured FPS-improving game build.
- Latest checkpoint resolves the isolated overlapping-XGKICK failure, including the game's branch-delay form. Original 14/32 and spread 8/32 eligible records now complete with every packet completion time matching; all repeat their cold outputs across 256 warm executions. Spread case 19 finishes nine packets at cycle 627, with two packet bytes still different. Both diagnostics remain accepted=0. The earlier spread-failure entry below is history.
- Seven new pre-issue wait tests pass, alongside eight transfer tests, pending-state import, 21 upstream tests and ABI/budget/XGKICK contracts. Final SHA `1B56D4693EF2EF5697C54384F313002FA5150AE8E01297911E2B733280BF7D0C`. The opt-in patch splits at transfer/branch boundaries, ages pending FMAC masks during waits and retains incoming hazards through short blocks. Branch-delay waits currently require NOP upper on the branch pair; unsupported forms reject compilation. Patch reverse-check passes. No game build, image, or gameplay FPS claim; tests/builds are terminal.
- Next: exact short-cycle scheduling and architectural-state export, unsupported pipelines/flags, and numeric mismatches (spread case 9 has 78 differing packet bytes, case 29 has 35). Measure the complete wait-enabled bridge before promotion; earlier 4.2x/6.8x isolated comparisons do not establish this version's speed. Keep the title/FPS feature and controls-enabled alert for the next genuinely FPS-improving game build.
- Latest isolated Play! checkpoint imports pending VF and supported flag events, extending original capture eligibility from one to 14 records. All 14 match packet completion times and repeat their cold results for 256 runs; arithmetic/memory/packet differences persist, so accepted=0. Public pending-import regression, eight transfer regressions, 21 upstream tests and ABI/budget/XGKICK contracts pass on SHA `63D79D7DB0D7431E8489E29CA4CFA00B114888AF02454EE4493540F348A74745`. Earlier one-record statements below are checkpoint history.
- Spread capture records 5/9/12/17 run with exact packet completion times, then case 19 fails explicitly on overlapping XGKICK at PC 6496/cycle 64. Implement a real pre-issue transfer stall without double-counting FMAC hazards; do not simply force the transfer to finish after issuing the pair. Case 9 also has 176 memory-byte/eight VF-word differences. Pending stores/VI/ACC/scalars/branches, active transfers and CPU-visible short-slice import/export remain unsupported. No game build or gameplay FPS claim; all test processes closed, no captures created.
- The isolated compiled VU now has an optional memory-event observer and a diagnostic streaming PATH1 timeline. Original capture case 19 matches all 16 packet completion times and final transfer time 2425 exactly; the apparent 2420/2425 gap was the final transfer tail. One packet byte still differs: the depth word of packed XYZF2 vertex data, packet 14 offset `0x68`, not texture or color data. This resolves transfer timing for that case, not arbitrary short slices or general compatibility.
- Eight compiled transfer regressions pass (early/late writes, preserved tag, end tail, memory wrap, linked blocks, I-bit suppression and relocated-code event PCs), along with the 21 upstream VU tests, Windows ABI regression and original budget/XGKICK contracts. Final test SHA `29709F90A6668746389D6CF033E97854B9791C978A6A433B3F9AF6C535B15489`. Both required source patches and BSD notices are retained under `play-vu-probe`; no PS2Recomp runtime change or game binary was made.
- Three 256-repeat comparisons including transfer observation/drain: current VU case 19 takes 25.036/31.819/26.854 ms; Play! takes 6.215799/6.443599/6.556299 ms (about 4.2x lower median isolated time). The earlier 6.8x result excludes this work and must not describe the current path. Each baseline run passes all 32 records with unchanged digest, and the diagnostic repeats its cold state, memory, packets and completion times on every warm run. One eligible record and unresolved flag/arithmetic/slice semantics mean no accepted optimization or gameplay FPS claim yet.
- Next: exact short-slice scheduling and pending-pipeline import/export, broader recordings, flag semantics, and the remaining numeric difference. The observer deliberately rejects special nested-branch/E-delay paths and overlapping transfers requiring a stall instead of silently dropping work. It is not a production replacement. All owned tests are closed, no new captures were made, and the disc image-artifact check found none older than 12 hours. The controls-enabled handoff is still pending.
- Isolated Play! VU probe now passes 21 upstream tests, budget/XGKICK checks and a new public Windows ABI regression. The pinned CodeGen overwrote all ten nonvolatile SIMD registers; synthetic arithmetic still returned correct results, explaining why earlier VU tests missed it. The public test demonstrably fails without the local patch (mask `0x3ff`, exit 6) and passes with it (mask zero). `play-vu-probe/codegen-win64-simd.patch` preserves these registers on Windows; CMake requires the patch and BSD notices are retained. Final test SHA `E8BA8C585E58A6764D491A8DC1031AC63A09A9C5052676B7A9E6D6C0F334025A`. No game integration or new game binary.
- One real recorded workload now runs in Play!: original capture case 19, the only record with an importable quiescent pipeline. ACC, Q/P/I/R, VI and termination PC match; one VF word, 28 memory bytes across 27 words, and one byte in 16 ordered/same-sized GIF packets differ. Unsupported STATUS bits `0xb00`, flag mirror retirement and 2420-versus-2425 cycle accounting remain unresolved. The diagnostic always reports `accepted=0`; deterministic repeatability is not compatibility. Both engines already use toward-zero rounding, so do not chase a nonexistent caller-rounding mismatch.
- After the Windows fix, three sequential 256-repeat comparisons measured case-19 totals of 23.621/32.560/23.489 ms in current PS2Recomp and 3.417799/3.829699/3.458299 ms in Play!. About 6.8x lower median isolated executor time is promising feasibility evidence, not an accepted optimization: the new path omits scheduling/streaming and some flag behavior. Baseline passes all 32 records with digest `75d4ff1e67bbbc4c`; the diagnostic reproduces its cold state/memory/packets on all 256 warm repetitions. Exact test identities and limitations are in `play-vu-probe/README.md`. Discard the initial nonsensical timing from before the ABI fix.
- Direct engine substitution is ruled out by measured API differences: `Execute(1)` can execute 66 pipeline cycles, quotas count instruction words, and XGKICK callbacks see block-start pipe time after the following lower store. Next build a cycle/graphics-aware bridge and compare memory/packets before throughput claims. Existing private captures contain mid-program state (including active PATH1 in several spread records); visible-register-only import would be invalid. No new game run or screenshot occurred in this investigation.
- The user's target is sustained 30 FPS in controllable first-level gameplay. 20 FPS is only an interim checkpoint requested within the next few hours, not acceptance or goal completion. Current verified measurements are still roughly 4 FPS; no delivery-time guarantee is established.
- Stop treating percent-level instruction tweaks as sufficient progress toward responsiveness. Investigate block-level VU execution and drawing cost reductions, preserving comparison paths and checking complete game workloads before claiming a gain. The prior phase split was about 57% VU / 30% GS, so both paths matter.
- No ready-made GPU backend or replacement VU engine was identified in the current upstream PR inventory. Evaluate a larger execution replacement in isolation; the earlier guest fault remains open independently.
- Flag-batch prototype test image `0622A760270F06710F8410330A331D7D88BBDBD3CC1D9EC8D73ADF6A70D7F2BC` passes 146 VU tests and both recordings at normal/1/8/16/64-cycle budgets with identical state/memory, cycles and digests. Normal-budget batched coverage is 50,596 / 13,722 pairs including cold pass. First three-round same-executable original comparison is negative: 1694.118 ms serial / 1738.007 ms batched, 2.591% slower, 1/3 wins. Do not promote this version.
- Cached-plan prototype `16F0F0D9BED5FED154AD5C99463A55A86A2A97A912B5AA80BC2D6C05787207B7` passes 146 VU tests and all ten capture/budget checks with identical state, memory, cycles and digests. Three alternating same-executable comparisons: original 1647.406 / 1562.436 ms (5.158% lower), spread 1362.890 / 1309.283 ms (3.933% lower), 3/3 wins each. This is insufficient for the required scale, not a gameplay FPS gain. Checkpointed as PS2Recomp `b7aface`, then removed from the active runtime along with its switches/counters. Keep the new incoming-flag/tail regression and public synthetic seed.
- Restoring the experiment returns the VU class layout to `fd41350`. The game candidate never contained the flag-batch experiment; do not relink old generated objects against the experimental class layout. The benchmark's independent `-CpuRasterProfile` switch is retained.
- Current candidate `61E24D60...` phase/CPU-raster run completed 2026-09-06 06:37 UTC: exit 0, vsync 1400, zero guest faults, New Game/NYC/native-block gates verified, startup restored and test closed. Across 181 presents (1100-1280), CPU drawing averages 62.668 ms: 11,342.902 ms total, 10,471.130 textured; triangle strips 6637.420, fans 4021.696, sprites 683.588 ms. This profile is not an FPS measurement.
- A single ignored, shallow Play! reference checkout exists at `.tools/Play-VU`, pinned to `83700b2c31e593bc94e845b4b31b797be84dda59`, with Framework, CodeGen, Dependencies and xxHash initialized. Reuse it and its single test build; do not add clones or binary backups. Next resolve arithmetic/flag/cycle differences and extend coverage to pending-pipeline records before game integration. The Windows CodeGen fix has a public reproducer but has not been submitted upstream. All owned test processes are closed; the game candidate remains unchanged and no screenshots were created.

## Live FPS Title

- Do not link, package, or launch a game build just for the title/counter. The user requires FPS-improving changes before the next gameplay build; include this queued feature in that build. This turn built tests only, not a new game executable.
- User requested `X-Men Legends` as the window title with a resident FPS counter. The change measures newly latched, valid game-frame presentations, excludes duplicate host redraws, samples real monotonic time once per second, and reports zero during no-frame windows. Warmup reads `X-Men Legends | -- FPS`.
- Focused deterministic tests cover 4 FPS among 100 host redraws, 30 FPS among 60 redraws, sample reset, stalls and multi-second frames. Both pass on test image `572A59384879D900A6ECB8864457E52475464875350F668D0EFA198181A1FF93`. Initial exact-double assertions failed at normal rounding precision and were corrected to a 1e-9 tolerance; title formatting already passed. The restored runtime also passes all 146 VU tests and ten exact capture/budget checks on preceding test-only image `602CBB3B...`. Game-level title verification is deferred to the next FPS-improving build.
- Current game candidate remains `61E24D60BF559B6BA7382E498905D6287171E8F3A5DF738D99CAB029E6D91D09`. No new game executable was linked or launched for this feature, and no game is left running. No new screenshot was taken; the bounded Play! reference is 31.79 MiB.

## Input During Testing

- User handoff gate: once a build with measured FPS improvement is ready for interactive testing, clearly announce **Controls enabled: ready for you to test**. Use the ordinary interactive launcher, state that the user can move/test combat, and stop further automated testing or replacement builds while awaiting their feedback. The user explicitly requests an alert and stalled work at that handoff. Do not silently enable controls and assume the user knows. Keep the goal incomplete; user verification is required then. Do not launch an unchanged or title-only build for this handoff, and never automatically restart a user-closed game.
- The recent automated benchmark runs intentionally set `PS2X_DISABLE_HOST_INPUT=1`; the Pad stub suppresses keyboard/controller reads in that mode. This explains the inability to move during those runs, not proof of a new input regression. Input has not been re-tested interactively this turn.
- `run-interactive.ps1 -RuntimeVariant Candidate -StartupMovieMode TitleGameplayFirst` clears inherited PS2X settings and enables ordinary controls. Use interactive mode when handing the game to the user. Clearly announce any controlled benchmark that disables input before launching it, and do not present such a run as a playable handoff. Never restart automatically after a user closes the window.

## Current Register-Forwarding Work

- Local regression `a36c659` adds 128 cases covering every ACC mask, four VI boundary values, an older pending ILW, old-value branch backup, ignored VI0 writes, four-cycle MADD results and zero/one-cycle slices. Explicit expected values pass before changing runtime; serialized before/after state and memory retain queued-baseline fingerprint `00d374d94b9195ad`.
- Experimental local runtime `fd41350` bypasses temporary ACC/VI queues only for one-cycle writes with empty incoming queues. Sequence/readiness metadata, canonical inactive slot contents and signed VI conversion remain identical. Pending/longer-latency writes retain the queued implementation. Final test image `362DA450EA93D41BF5550AF8FBE0E586C80DC068C7A40944A5BD52D0BFDEE65F`, map timestamp `0x6a9cfde7`, passes all 145 VU tests and both private captures at normal/1/8/16/64-cycle budgets with exact cycles, digests and coverage. Whole-game acceptance remains pending the fault investigation below.
- Seven alternating comparisons against queued baseline `7A353F68CDA6E8198501D2AD1BE8AD6A8A7BEE2EAEBE657B23D0777CDF1ECB8B`: original medians 1638.743 / 1596.701 ms (2.566% lower, 6/7 wins), spread 1391.683 / 1360.304 ms (2.255% lower, 7/7). These are VU-only gains. The fixed comparison JSON contains spread results.
- Game candidate links as `61E24D60BF559B6BA7382E498905D6287171E8F3A5DF738D99CAB029E6D91D09`, 163,421,184 bytes, PE timestamp `0x6a9cfeb1`. The first check logged an unmapped guest PC `0xa3a4f0`, return address `0x396e84`, after present 1152. Fixed `gameplay-failure.err.log` / `gameplay-failure.json` preserve that failure. Its cause is not established; do not attribute it to allocation failure or dismiss it because the host exited zero.
- The user confirmed manually closing the subsequent test and requested a restart. That interruption is not evidence of a crash. The unchanged candidate's restarted test completed at 2026-09-06 06:05 UTC: exit 0, vsync 1400, zero guest-fault lines, New Game/NYC/native-block gates verified, startup restored. 128 presents took 32.6763735 seconds (3.91720 FPS), total 252.7986486 seconds. This shared-host observation is not a demonstrated speedup, visual fix or new input test. All owned game/test processes have ended.
- The benchmark now rejects missing-PC, missing-target and program-execution faults even when the host exits zero. Its focused gate regression covers fault prefixes and required workload markers. Next: investigate the preserved fault with controlled baseline evidence before promoting register forwarding or submitting its runtime change upstream. Do not restart automatically after another user close.
- Contribution checkout `C:/Programming/GitHub/PS2Recomp` is on new branch `codex/vu-one-cycle-registers` from freshly fetched upstream/main `14b1e5c`. It has only draft public tests so far: both VU units plus repeated register/load/store/branch scheduling. No upstream runtime change or new PR has been made. Upstream lacks the local queue masks, so the port must independently test and measure its empty-queue check rather than claim local replay gains apply unchanged.
- The fixed execution profiles still belong to `E9F38500...` / `0x6a9cf915`, not the latest test image. The last whole-game phase profile is the prior `1600B446...` candidate. Do not attribute their addresses with the new map or label them as current-candidate measurements.

## Earlier Store-Optimization Checkpoint

- Accepted and integrated PS2Recomp `ade0d08` on `codex/xmen-legends-bringup`. The native-block-specific change is not a standalone upstream PR. Existing binary/log/framebuffer slots are reused, with no new clone, worktree or executable backup. Protected files and unrelated edits remain untouched.
- At the user's request, restarted the same accepted `BF9DED00...` game candidate. The 2026-09-06 05:28 UTC check exited 0 at its vsync-1400 limit, passed New Game/NYC/native-block gates, and restored startup. 128 presents took 27.3831546 seconds (4.67441 FPS); early overlap with the finishing build/regression tests means this is not a controlled performance comparison. No new visual or manual-control verification is claimed. All owned build/test/game processes have exited.
- The interrupted BelowNormal, one-worker test build completed successfully. Final test SHA-256 `E9F3850029A44174E1075B8F31B61748230FF98E21073FC41D950909BCA8E711` passes all 144 VU tests and both private recordings at normal/1/8/16/64-cycle budgets, with unchanged replay digests and cycles. Both address-filter and word-filter store trace checks pass.
- Public regression `983d723` covers all store masks, address wraparound, overlapping upper writes and in-flight PATH1 reads. The candidate bypasses transient one-cycle store queues only inside guarded native blocks; interpreted/individual-pair execution and targeted diagnostics retain queued stores. Full-mask writes use one 16-byte copy.
- Final seven-round alternating comparisons against `4D876140E14717E5E2A8D20E99B752BEFBB5C4DB8338F93C253FAA7DF5FF5022`: original medians 1656.341 / 1614.567 ms (2.522% lower, 7/7 wins), spread 1370.188 / 1342.890 ms (1.992% lower, 7/7). Full-width store bodies use one `vmovups` write; 106 unique direct-store bodies have no calls to `queueStore`. These are VU-only results, not whole-game percentages.
- Candidate SHA-256 `1600B44639E00AD71C3E877F5957FADBA1BE0C2FB2CAE00A6A214B09E5F6CFEE`, 163,420,672 bytes, PE timestamp `0x6a9cfa8a`. BelowNormal link and PE validation pass. The 05:34 UTC first-level run exits 0 at vsync 1400 with all startup/level/block gates verified and startup restored. 128 presents take 26.0671768 seconds (4.91039 FPS), still far below practical responsiveness. Native present 1280 confirms textured NYC/Wolverine and the existing black props, missing foliage, red player disk and malformed HUD. No visual fix or new manual-control verification is claimed; primary/staged executables are unchanged.
- Fixed execution profiles now match final test image `E9F38500...`, map timestamp `0x6a9cf915`. Three 2048-repeat runs per recording preserve exact results, with zero failed/dropped samples and all in-module IPs mapped: original 814 total / 5 external / 809 mapped; spread 328 / 4 / 324. `run` is 6.18% / 12.35%, generic retirement 4.45% / 5.86%, readiness scanning 4.57% / 4.94%. Matched `run` disassembly shows VF save/restore, decoded-pair copying and ACC queue setup among sampled sites. Do not repeat the rejected by-reference decode or native direct-destination trials.
- Next investigate eliminating the generic one-cycle ACC/VI transient queues, preserving sequence/readiness fields, pending older writes, branch-read backup and fault behavior. The same mechanism exists upstream, making an independently measured/tested generic fix a possible focused PR. No such change is implemented yet. Block-wide flag scheduling is a larger alternative, but requires explicit incoming sticky/CLIP flags, pending-tail and short-slice proofs; do not discard flags merely because a block has no flag reader.
- Refreshed whole-game phase profile of `1600B446...` completes at 05:39 UTC, exit 0 / vsync 1400 / all workload gates / startup restored. Its 59 complete same-thread windows from ticks 1102-1397 cover 61,433.304 ms: VU exclusive 35,163.886 ms (57.24%), GS 18,382.923 (29.92%), other guest 3,836.094 (6.24%), transfers 2,699.289 (4.39%), VU reset 569.074 (0.93%), events 456.418, scheduler 271.598, waits 0.098. VU remains the primary performance target, with drawing secondary. Profiling is not an FPS benchmark. Both game runs and all owned build/replay/profile processes have ended.
- Final workspace inventory is 8.152 GiB / 30,632 files. No disc images are older than 12 hours. The one verification frame reused the present-1280 PPM/PNG slots; no additional image series or runtime backup was created. Existing protected artifacts remain untouched.

## Earlier Packed-Arithmetic Checkpoint

- PS2Recomp `1395fb9` normalizes supported native arithmetic lanes with packed widened results. Interpreter semantics, delayed writes, cross-product fallback, and non-AVX2 paths remain unchanged. All 143 VU tests pass, including full-state arithmetic comparisons entered from two caller rounding modes, plus both private captures at normal/1/8/16/64-cycle budgets. VU execution always enforces toward-zero rounding; the caller-mode checks cover restoration, not two internal arithmetic modes.
- Seven alternating comparisons against accepted packed-product baseline `A21CC04F...` improve original replay by 5.147% (1744.672 / 1654.873 ms) and spread by 5.542% (1474.394 / 1392.689 ms), seven wins each. These are VU-only gains. Test SHA-256 `650795E6440917576E0A1709AF0D79AAC49F0F29BF4CCC4747B9F2170EB480F7`; complete identities and binary inspection are in VU-REPLAY.md.
- Integrated candidate SHA-256 `BF9DED00BE2E4387B2DEF8640EF8A36D62781B4C9F9519FE26FA88A84F74037F`, 163,360,768 bytes. The 05:02 UTC first-level check completed with exit 0, vsync 1400, New Game/NYC/native-block gates verified, and startup restored. 128 presents in 27.0317654 seconds measured 4.73517 FPS. The preceding accepted packed-product candidate measured 4.63 FPS; these are not controlled whole-game comparisons.
- Current native framebuffer still shows NYC and Wolverine with black props, missing foliage, red player marker and malformed HUD. No graphical fix or new manual movement/combat verification is claimed. The user previously confirmed control and basic combat; practical responsiveness remains the unfinished acceptance requirement. SFD is out of scope.
- The earlier `0x4c004000` guest-PC error occurred in a run the user reported closing. It did not recur in either subsequent complete first-level run; its cause is not proven. Investigate if it recurs, without treating every manual window close as a regression or automatically restarting it.
- Existing comparison/build/log/image slots are reused. Workspace is 8.008 GiB / 30,419 files; disc captures are younger than 12 hours. No additional clone, worktree or executable backup was created; unrelated edits and protected cleanup targets remain untouched. Primary/staged game executables remain unchanged. Native-specific packed arithmetic is not submitted as an unused upstream helper; PR #252 remains the independent pending-entry contribution.
- Final test-only `152b089` explicitly verifies caller-rounding restoration. Final test image `784D74F1BF49C65E9B863E14A2F85CB56DC6629F825EFCF7043F3AB45FDFF2E9` passes all 143 tests and ten replay checks; candidate game unchanged. Refreshed fixed execution profiles match its map timestamp `0x6a9cf474`: original 638 samples / 5 external, spread 269 / 5, every in-module sample mapped and zero failures/drops. Instruction execution is 27-28%, pipeline retirement about 14%, and FMAC helpers about 11% of in-module VU samples. `run`, generic retirement and readiness scanning remain significant individual symbols. Next inspect their matched hot instructions and reduce repeated execution/scheduling work at a larger granularity; do not repeat rejected by-reference decode, queue-index or kernel-ranking trials. All owned build/test/game processes have exited.

## Earlier Pending-Entry Checkpoint

- Final local test image `B4C96193B519A08B3E2F7584DC44C7EB3D7B068D6E90EF57C8D1FDC923B82D40` includes the retained upstream regression and passes 141/141 VU tests plus all ten capture/budget checks. This test-only addition leaves the game executable unchanged. All owned build/test/game processes are closed.
- Accepted runtime `280147c` clears pending VU entries directly, preserving all values, masks, deadlines and retirement ordering. Seven alternating comparisons against `A7070757...`: original 2120.947 / 2049.752 ms (3.357% lower, 7/7 wins); spread 1637.766 / 1532.869 ms (6.405% lower, 7/7). Test image `79F54B78...` passed 140 VU tests and all ten capture/budget checks. Matched disassembly confirms removal of aggregate stack temporary/reload sequences.
- Candidate SHA-256 `67C66DEBBB0286EC4A90EF40AC459FB4465CE91A68D99E6CD330919F624835BB`, 163,363,840 bytes, PE timestamp `0x6a9ceab1`. The 04:27 UTC first-level test exited 0 at vsync 1400, verified real New Game/NYC/native blocks, closed its own process and restored startup. It measured 128 presents in 29.1998644 seconds (4.38358 FPS), total 231.2623991 seconds. Whole-run block pairs 775,779,683 differ from the prior run, so the 4.14-to-4.38 FPS observations are not a controlled speedup claim. Primary/staged unchanged.
- Runtime-owned present 1280 was inspected in the reused PPM/PNG slots: textured NYC/Wolverine remain visible, with the known black props, missing foliage, solid red player marker and malformed HUD. No visual fix or new manual-control test is claimed. Practical first-level playability remains unmet.
- Submitted [PR #252](https://github.com/ran-j/PS2Recomp/pull/252), `1e73f01`, branch `codex/vu-pending-entry-clears`, directly based on upstream `14b1e5c` in the existing contribution checkout. All 426 upstream-based tests pass before and after the change. Regression covers all 16 masks, signed-zero/NaN/infinity/subnormal raw bits and one-cycle VF/VI/store visibility. Its first draft incorrectly passed the cycle budget in the TOP argument slot; corrected before baseline validation, with an explicit cycle-count assertion. This was a test error, not an API difference or runtime failure.
- Rejected FMAC helper inlining is removed by `e0abe98`: 8.687% slower original (0/7 wins), 9.961% slower spread (1/7). Do not repeat or promote it. Refreshed fixed execution profiles now belong to pre-clear restored test `98665AEB...`, timestamp `0x6a9ce90f`, not the current linker map. External copy hotspot is gone; instruction execution and delayed-result queue work dominate remaining VU cost. Reset remains only 0.895% of measured gameplay wall time.
- Next: continue from the accepted direct-clear code, keep input/combat functional, and target substantial remaining execution overhead using matched code/profile evidence. Do not optimize replay setup, repeat rejected operand/queue-index/kernel-ranking experiments, or return to SFD. Existing test/build/image slots are reused; workspace inventory is 8.000 GiB / 30,373 files, and all current disc captures are younger than 12 hours. Protected cleanup targets and unrelated edits remain untouched. Older entries below are historical.

## September 6 Restart Checkpoint

- The user-requested restart completed at 04:11 UTC: exit 0 at vsync 1400, real New Game/NYC/native-block workload verified, startup restored, and game closed. Candidate SHA-256 `74C32DFFDD87EF3AB05784D6BDB57ABB63A1EC76976061175EA75732540E8AFB` includes reset profiling but not the experimental math helper. Unprofiled 128 presents took 30.9132713 seconds (4.14062 FPS), total 234.3204689 seconds. This shared-host observation is not a controlled speedup claim; no new visual inspection or input test was performed.
- `054b629` measures scheduler initialization separately. The prior completed phase run contains 66 reports ending at ticks 1100-1388, 69,356.215 ms wall: vector execution 59.60%, drawing 28.15%, scheduler reset 0.895% (288,813 calls, 620.54 ms). Leave reset behavior unchanged; its measured cost does not justify that optimization lead. Fixed `gameplay-phase.*` holds this run, not the older coverage run.
- Experimental checkpoint `3b728bb` extracts the unchanged exact FMAC normalization body into a translation-unit-local inline helper. BelowNormal single-worker build completed successfully. Test SHA-256 `4452C95C2E58DA6F2DF6177D9DCB1CF8EA6D87955E4C54C165FD072C802210B5` passes 140/140 VU tests and both captures at normal/1/8/16/64-cycle budgets, with unchanged digests and cycles. This is correctness evidence only; do not promote the change to the game executable or claim a performance gain yet.
- Next: inspect emitted code for helper-call removal and retained widened arithmetic; run seven alternating comparisons per capture against the existing `ps2x_tests.flag-pack-base.exe` comparator, SHA-256 `A7070757320A690E255846B8702C6C8785BB8B133435E569E620D2F49BDE6677`. Keep the experiment only with repeatable benefit; otherwise undo only its scoped math change. The game candidate and primary/staged executables are unchanged by this experiment.
- The restarted game, compiler build, and correctness tests are all terminal. No new screenshot, executable slot, clone, or worktree was created. Existing unrelated edits remain untouched. Entries below describe earlier checkpoints.

Practical first-level playability remains unmet. The updated opt-in candidate completed at 2026-09-06 03:46 UTC: exit 0, verified New Game/NYC/native blocks, 128 presents in 31.493247 seconds (4.06436 FPS), total 261.275584 seconds. The prior user-requested unchanged-candidate rerun measured 3.71035 FPS. These shared-host observations do not establish a sustained speedup. Startup was restored and both tests closed normally.

## Current XGKICK Work

- `49347fe` adds bounded process-local external module/RVA attribution. All external samples in three runs per capture resolve, mostly to VCRUNTIME140 copy/clear loops. Reporter `-External -All` retains explicit unresolved/dropped accounting and handles per-run module IDs; ordinary map reporting rejects mixed executable timestamps.
- `304e2a1` removes the 64 KiB temporary zero/copy in `startXgkick`, preserving metadata resets and every transfer-cycle read. A new long/short/growing-packet test passes before and after. Optimized build passes 139 VU tests and all ten recorded capture/budget checks. Matched binary inspection confirms removal of the bulk calls and large stack allocation.
- Seven alternating timings improve original by 15.467% (2727.913 / 2305.994 ms) and spread by 17.684% (2106.688 / 1734.131 ms), 7/7 wins each. Baseline comparator is now `72653FAE...`, optimized test image `F056E7FE...`; full identities are in VU-REPLAY.md. These are VU-only results, not game FPS evidence.
- Integration passes: candidate `13715A67FF82743E96E8F9805A4E69696AEB5AE0868669D35FC5F4F91A3685F2`, 163,368,448 bytes, timestamp `0x6a9ce0d6`. Primary/staged unchanged. Runtime-owned present 1280 shows textured NYC/Wolverine with the known black-prop/foliage/HUD/effect defects. No graphical fix is claimed. New whole-run block-pair count 630,871,823 differs from the prior 340,133,123, so do not call the 3.71-to-4.06 FPS pair a controlled speed comparison.
- Upstream submission complete: [PR #251](https://github.com/ran-j/PS2Recomp/pull/251), commit `9649cf4`, branch `codex/vu-xgkick-storage` based directly on `14b1e5c` in the existing contribution checkout. Only two files, no local profiling/private game data. Its standalone regression checks exact bytes and transfer completion cycles 511/514/523. All 426 upstream-based tests pass before and after the optimization. Existing #250 branch is preserved.
- Fixed external-profile logs are pre-optimization image `06542E83...`, not the current linker map. Original: 1042 execution / 138 external; spread: 380 / 62. All zero-failure/zero-overflow and exact replay. Do not attribute their in-module RVAs using the new map.
- Older checkpoints below are history, not the current executable/log identities. SFD remains out of scope. No new clones, worktrees, or image slots were created; existing images are younger than 12 hours and protected cleanup targets remain untouched.
- Next: audit remaining bulk state resets (especially scheduler initialization versus per-slice execution) and their actual gameplay call frequency. The replay profiler covers warm `run`, not `execute`/snapshot setup, so do not assume it measures those costs. Preserve the first-level control/combat path and avoid more small operand/slot/kernel-ranking experiments. Refresh execution profiles only when needed for the next decision; current fixed logs intentionally preserve the pre-optimization attribution evidence.
- Checkpoint cleanup: 8.004 GiB / 30,327 files; all owned compiler/test/game processes are terminal. The updated game candidate is opt-in. Unrelated outer script edits and nested header/PPM are untouched.

## Resume Point

- Gameplay coverage is now measured, not inferred from replay: `955b393` adds opt-in execution-thread VU coverage windows. Its original 131-test checkpoint passed both captures with reporting enabled. Corrected `run-gameplay-benchmark.ps1 -CoverageProfile -PhaseProfile` completes, verifies all workload gates, closes its process, and restores scripts. The first nine tick-1100-1388 windows measured 66.21% native pairs, including 45.26% in compiled blocks and 20.95% in remaining native-pair kernels. These are instruction counts, not time percentages; the latest repeat is recorded below.
- Initial phase evidence was 73 reports ending at ticks 1100-1388, 76,239.624 ms wall time; VU 63.03%, GS 25.43%, guest 5.63%, transfers 4.83%, waits negligible. The later restart below overwrote the fixed `gameplay-phase.*` report. No FPS is calculated for coverage/phase-instrumented runs. The latest unprofiled candidate measures 2.51 FPS; practical playability is still unmet.
- The 128-private-pair experiment is rejected and the selection is restored to 64. Seven alternating rounds measured 2516.006 / 2592.948 ms on original (3.058% slower) and 2046.555 / 2120.754 ms on spread (3.626% slower), only 1/7 wins each. All 131 then-current tests and both normal/1/8/16/64-cycle replays remained exact. Compiled-block coverage was unchanged. No game executable was replaced by this experiment.
- Residual-ranked 64-pair selection is also rejected: all 133 then-current tests and both sliced captures passed, but seven-round medians were 2294.803 / 2338.945 ms original (1.924% slower) and 1963.659 / 1980.650 ms spread (0.865% slower), 2/7 wins each. All 26 block bodies plus dispatcher were byte-identical; normal interpreted counts fell as predicted, while short-budget coverage worsened. The original weighted 64-pair / 16-block selection is restored. Do not pursue more count-only kernel shuffling.
- Current game candidate integrates `053b1bc`, SHA-256 `CE26EA1E3D4586D61D6AFA855AD76E625174C9703965669CD155386C35488DAB`, 163,364,352 bytes. It replaces the previous opt-in `CE7E1C90...` image; primary/staged hashes are unchanged. The reused present-1280 PPM/PNG now shows this candidate's completed unprofiled first-level check. Current comparison/test identities are recorded below.
- Reporting corrections: initial new C++ tests lacked required assertion messages; fixed and rebuilt. File-backed report-test cleanup was denied, so `tests/.vu-coverage-test.log` is protected and must not be retried; regression tests now use memory only. The first coverage game reached vsync 1400 but finalization failed reading its still-open log writer. Both writers are now disposed before parsing, and a same-build full repeat passed. Preserve the distinction between that wrapper failure and a game crash.

### Deferred Output Checkpoint

- PS2Recomp `de8b54a` commits the synthetic block and regression test only. Runtime implementation sources match the accepted version. Final inventory: 8.004 GiB / 30,290 files, including the one additional active synthetic-block build target. No new executable slots, screenshots, or worktrees; protected deletion targets remain untouched. All owned builds/tests/profile processes are closed. Primary/staged/candidate game hashes remain unchanged.
- Direct-to-pending compiled upper VF output was correct in 136 VU tests and all ten capture/budget checks, but performance was mixed. Seven-round original medians: 3054.288 / 3080.983 ms (0.874% slower, 4/7 wins); spread: 2233.983 / 2174.451 ms (2.665% faster, 6/7); repeated original: 2324.312 / 2362.387 ms (1.638% slower, 2/7). Removed the three runtime source changes without a gameplay relink. Experimental SHA `F07212539420F86C670AD955532B41A6C91ECFC271D389908DF157544CD16137`; current comparator slot is accepted `A66D5E855A30F810A6AB45CC77FBEAB712D70FE69101EB1001A4E51ED523A6D7`.
- Retain one synthetic block plus 112 exact pending-state cases covering old-value stores, suppressed loads with address-register side effects, distinct load data, partial destinations, and integer-conversion output bits. Public block count increases from 10 to 11; private block/pair selection is unchanged. Restored test image `09BF14A27C3D58AF9ECB9461092ADB2E469BDF78548B595398951D902F9DEE81` passes 136/136 VU tests and both captures at normal/1/8/16/64-cycle budgets. Rebuild stayed BelowNormal, one worker, affinity 0xF, 2048 MiB cap.
- Refreshed execution-only profiles: three 2048-repeat runs per recording; matching image/map timestamp `0x6a9cdb48`, exact results, zero dropped/failed samples. Fixed `execution-profile-original.*` / `execution-profile-spread.*` are current; the unsuffixed profile is historical. Original has 952 in-module samples across 237 symbols, spread 386 across 128. Pipeline queue/retirement families account for 21.85% / 29.02% of in-module samples; FMAC flag helpers 14.39% / 13.73%. These are exclusive sampled-IP families, not whole-game timings.
- The reporter's new tested `-All` mode reconciles every in-module sample; Top=100 previously omitted 180 original and 28 spread samples. Next add bounded process-local external module/RVA attribution: 162/1114 original execution samples (14.54%) and 119/505 spread samples (23.56%) are external and currently unidentified. Close this gap before choosing another larger execution change. Do not repeat small copy/operand/cache/selection experiments. Larger changes must preserve delayed observable state and graphics-transfer timing; the Play! reference does not justify discarding sticky flags or pending results.

### Interpreted Operand Checkpoint

- Rejected dynamic FMAC normalized-input reuse. All 135 experimental VU tests and both captures at normal/1/8/16/64-cycle budgets passed, but seven alternating timings did not show a reliable improvement. Original: 3428.418 / 3556.435 ms, 3.734% slower, 1/7 wins. Spread: 2822.932 / 2732.752 ms, nominally 3.195% faster by separate medians but only 2/7 paired wins. The shared host varied substantially. All runtime changes were removed; no gameplay relink was performed.
- PS2Recomp `711d5d2` retains the independent 288-case masked/aliased FMAC regression test. The restored runtime rebuilt BelowNormal with one worker, affinity 0xF, and the 2048 MiB cap. Final test image `A66D5E855A30F810A6AB45CC77FBEAB712D70FE69101EB1001A4E51ED523A6D7` passes 135/135 VU tests, both captures at normal/1/8/16/64-cycle budgets, and both budget-trace fixtures. Runtime sources match `053b1bc`; the test is the only nested source change in this checkpoint.
- Current comparator `ps2x_tests.flag-pack-base.exe` holds accepted `053b1bc`, SHA-256 `78076FBB69E84003A4C1C1B406C196413BFB43FAB8C7313992FC9DF74C91BFBB`. The rejected experimental test image was `D43D66CB731564E506EA8EE485276A857E58CD9EA72596277BD13F0A33D8CC3E`. Fixed comparison JSON contains the spread experiment; full medians and decisions are in `VU-REPLAY.md`.
- Latest unchanged-candidate gameplay rerun: exit 0 at vsync 1400, New Game/NYC/native blocks verified, 128 frames in 50.9136505 seconds (2.5140605 FPS), total 283.8631807 seconds. Startup package restored. Existing present-1280 PNG remains the earlier same-candidate inspection, not a new screenshot of this rerun.
- Final workspace inventory: 7.991 GiB / 30,266 files. Existing executable/report slots were reused; no new screenshots or worktrees. Active disc images are all under 12 hours old; protected cleanup targets were not retried. Owned build/test/game processes are closed. Unrelated existing edits remain untouched.
- The subsequent deferred-output checkpoint above tested direct delivery into pending storage; it is rejected. Do not repeat it or the rejected cached-slot, decode-by-reference, and count-only selection experiments.

### Budget Trace Checkpoint

- Completed candidate gameplay check: BelowNormal/one-worker/affinity-0xF/2048-MiB guarded link succeeded with existing raylib duplicate-symbol and `/FORCE` warnings; PE32+/x64 validation passed. `run-gameplay-benchmark.ps1 -CaptureFrame` reached the real New Game handler and NYC package, exited 0 at vsync 1400, and restored all four startup entries. Presents 1152/1280 arrived at 193.0931207 / 225.2273819 seconds: 32.1342612 seconds for 128 frames, 3.9832875 FPS. Total elapsed 245.00612 seconds. This shared-host run does not show a meaningful gain against the earlier 4.14 FPS sample.
- Runtime-owned present 1280 was inspected in the existing PNG slot: textured New York and Wolverine remain visible; black props, missing foliage, red solid player marker, and malformed HUD elements remain. No new rendering fix is claimed. Host input was disabled for the benchmark; earlier user-confirmed movement/combat remains prior-build evidence, not a new manual input test. All owned test/game/build processes are closed. Workspace: 7.991 GiB / 30,247 files; protected cleanup targets were not retried.
- PS2Recomp `053b1bc` removes two unconditional clears of the 2,432-byte local debug trace array on every VU run. The matched pre-change binary shows a `memset(0x980)` followed by the array initializer; the final map/disassembly confirms both are gone. Only fully written entries are retained/read. No guest timing, queue semantics, instruction execution, or selection changes.
- Test SHA-256 `78076FBB69E84003A4C1C1B406C196413BFB43FAB8C7313992FC9DF74C91BFBB`: 134/134 VU tests and both recordings at normal and 1/8/16/64-cycle budgets pass. `tests/test-vu-budget-trace.ps1` verifies every emitted PC/opcode/signed-register field for short (3-entry) and wrapped (40 issued / 32 retained) traces with diagnostics enabled. `tests/test-vu-residual-selection.ps1` passes its memory-only validation suite.
- Seven alternating comparisons of trace initialization removal: original normal 2334.653 / 2315.661 ms (0.813%, 4/7 wins); spread normal 2007.371 / 1960.860 ms (2.317%, 5/7); original 8-cycle 2060.460 / 1912.508 ms (7.181%, 7/7); spread 8-cycle 1711.016 / 1626.306 ms (4.951%, 5/7). Normal gains are modest; short budgets measure repeated-call overhead, not gameplay FPS. `compare-vu-blocks.ps1` now accepts/reports `-SliceCycles` for matched budget comparisons.
- Comparison slot `ps2x_tests.flag-pack-base.exe` now holds accepted `633cf92`, SHA-256 `2F9FA03552A04BC007AE7B376F676ABDFD2208ED8D3473AAA7D7EBCEDDCEC6B7`. Older identities below are historical. The rejected residual-ranked recipe and its two small input CSVs remain private for reproducibility; no extra executable slots or worktrees were added.
- Next performance work should target shared execution overhead and use the actual gameplay call/budget mix. Cached decode by-reference was already rejected at about 4% slower (see earlier replay notes); do not repeat it. These local debug-buffer changes do not apply as an independent upstream PR because the game-specific budget trace is bring-up code.

### Residual Dispatch Checkpoint

- PS2Recomp commit `633cf92` contains the diagnostic and regression coverage. It does not change the selected kernels or game executables.
- Final normal test build restored: pair profiling OFF, private pairs 64, private blocks 16, original weighted recipe unchanged. Test SHA-256 `2F9FA03552A04BC007AE7B376F676ABDFD2208ED8D3473AAA7D7EBCEDDCEC6B7`. All 133 VU tests and both recordings at normal and 1/8/16/64-cycle slicing pass again; requesting residual export from this uninstrumented build is correctly rejected. Primary/staged/candidate game hashes are unchanged. Workspace inventory: 7.988 GiB / 30,209 files, including the policy-protected obsolete batches below.

- Added `PS2X_ENABLE_VU_PAIR_PROFILE` (default OFF), scoped only to the VU core/replay compilation units. `PS2X_VU_REPLAY_RESIDUAL_EXPORT` records the cold replay pass at its actual requested budget, after dependency waits and outside successful native blocks. Native and interpreted pairs are separate; PC plus both instruction words distinguish code versions. Storage is capped at 4,096 keys, overflow fails verification, and scoped ownership prevents warm passes or another VU from contaminating counts. One-cycle instruction export cannot be combined with residual profiling.
- Instrumented test SHA-256 `5C5E7D0039CE3970A889B67F2E66B95BCC5B5BBEC6B24B0511EAFB1F49D75E93`: 133/133 VU tests pass. Both 32-case recordings pass normal and 1/8/16/64-cycle replay with the diagnostic enabled. Each export asserts its cold counts times the number of passes equal total pair counts minus compiled-block pairs. Conflicting one-cycle/residual export is correctly rejected. Normal-budget counts: original 322 PC/word keys, 6,578 native / 1,265 interpreted; spread 389 keys, 1,976 native / 3,993 interpreted. Digests remain `75d4ff1e67bbbc4c` and `6c13c7a10069aeef`.
- Private CSVs are `disc/vu-replay-residual.csv` and `disc/vu-replay-spread-residual.csv`. They contain retail instruction words and must not be committed or shared. Normal-budget passes overwrite the same slots after slice-budget verification. Diagnostic-build timing is not an optimization benchmark.
- The reused `ps2x_tests.flag-pack-base.exe` comparator now contains accepted `955b393` with 64 private pairs, SHA-256 `7DDD7C0294EEE2B785AE8CC3E9475AB07EF76F24855F056782C3329561CEE4BA`. Earlier comparator identities below are historical. Rejected 128-pair test identity: `5A53509EC83FD3E2AD8E88284FBCC86D5521BC540A2DE08C694EF92E36AF4BDD`.
- The explicitly requested gameplay restart completed with unchanged candidate `CE7E1C90...`, exit 0 at vsync 1400, verified real New Game/NYC workload, and all four startup entries restored. Latest fixed gameplay profile has nine tick-1100-1388 windows: 388,831,787 native / 198,451,920 interpreted, 265,818,240 block pairs; 66.2085% native / 45.2623% block coverage. The 116 phase reports ending within that interval total 132,427.113 ms: VU 64.02%, GS 24.28%, guest 5.51%, transfers 5.19%. Instrumented shared-host times do not supersede the 4.14 FPS unprofiled sample. No game remains running.
- Cleanup identified 17 unused 128-pair batch directories (134.63 MiB), indices 68,72,76,80,84,88,92,96,100,104,108,112,116,120,124,128,131, plus their generated `.vcxproj`, `.vcxproj.filters`, and `.cpp` files under the active build's `ps2xRuntime` directory. Removal was blocked by policy; leave these specific targets untouched and do not retry or bypass the denial. Keep active batch 67. No extra executable slot, screenshot, clone, or worktree was created.

### Operand-Reuse Checkpoint

- Resumed validation accepts `119706f` operand reuse as a modest offline improvement: all 129 VU tests and both recordings at normal and 1/8/16/64-cycle slicing pass. Final-image seven-round comparisons against `110ed3a` measured original 3308.108 / 3232.464 ms (2.287% lower, 6/7 wins) and spread 2558.118 / 2519.449 ms (1.512% lower, 5/7 wins). Native pair/block coverage is unchanged. Matched image/map `0x6a9cbcfe` confirms widened double multiplication, not the rejected narrowed flag calculation. See `VU-REPLAY.md` for identities and the fresh 311-in-module-sample profile.
- Current opt-in candidate now integrates `119706f`: SHA-256 `266866D41A80660F63215B1B803D534AA490AA11AF3D2F10E00FCCE49838CD50`, 163,364,352 bytes. BelowNormal / affinity 0xF / one-worker / 2048 MiB link succeeded with the existing raylib duplicate-symbol and `/FORCE` warnings; PE32+/x64 validation passed. Header audit found only a nested helper type/private signatures, no new object data members; runner/PCH compile records retain native pairs/blocks ON, native upper OFF, `/O2`, runner IPO OFF. Primary/staged binaries are unchanged.
- Completed restarted game test: real New Game handler and NYC package verified, 597,644,543 native block pairs, normal exit 0 at vsync 1400. Presents 1152 and 1280 arrived at 188.6173256 and 219.518381 seconds: 30.9010554 seconds / 128 frames, 4.1422533 FPS. Shared-host single run, not a controlled gain against older samples. Current runtime-owned `disc/gs-present-1280.ppm` was converted into the existing PNG slot and inspected: textured New York/Wolverine, existing black props, missing foliage, HUD/effect defects. No visual fix or practical playability claimed; host input was disabled for this automated run.
- The preceding run was closed by the user accidentally, as explicitly confirmed, and restarted only on request. It exited 0 after present 1152 but before 1280; the benchmark correctly rejected it and produced no FPS value. The completed retry overwrote the fixed report/logs. No automatic relaunch behavior was added.
- Final housekeeping: all owned builds/tests/game processes are closed; all four startup ZIP entries match the preserved retail scripts. Primary/staged hashes remain unchanged. Workspace inventory is 7.860 GiB / 29,857 files. Existing test/game/image slots and logs were reused; no new clone or executable slot was created. Previously policy-protected artifacts remain untouched, and unrelated worktree edits were preserved.
- Next performance work: measure optimized/interpreted coverage inside the steady gameplay interval before expanding recipes or repeating small queue tweaks. Current whole-run counts include startup (974,217,359 native pairs / 640,806,485 interpreted), while the offline recordings have substantially different coverage; these are not comparable phase-scoped workloads. The latest profile still shows pipeline bookkeeping and arithmetic normalization costs. Preserve exact delayed writes, sticky flags, and PATH1 timing when pursuing larger compiled execution savings. SFD remains out of scope.

### Prior Stop Checkpoint

- Stop checkpoint after the disconnected build: PS2Recomp `119706f` is committed/pushed but remains an experimental native FMAC operand-reuse change, not promoted to gameplay. The original build failed CMake recipe validation because the new synthetic PCs used eight hex digits; corrected to the required four-digit format. The retry built `ps2x_tests` successfully at BelowNormal / affinity 0xF / one worker / 2048 MiB, then passed all 129 VU tests, including 3,216 arithmetic seed/start cases. Test SHA-256 `626C8504544A80009ED8923DDB8B9B9277019A6E26AF51F13F1B7C4806817B2C`. No game was launched, and primary/staged/candidate hashes remain unchanged. Stop here per user request.
- Resume validation before accepting operand reuse: replay both captures at normal and 1/8/16/64-cycle slicing; audit coverage after expanding public blocks from eight to ten (26 total, 16 private); repeat alternating timing on both workloads; inspect the widened arithmetic in the final matching binary/map. The first `/fp:fast` attempt lost an underflow sticky flag; the current native-only `float_control(precise)` guard fixes the existing regression without changing the interpreted oracle. Do not remove that guard on the basis of normal-value tests.
- Preliminary operand-reuse timing belongs to the earlier pre-expanded-test image `FC095762F3C6D82F6AB2749FEF2A8AC8FBF15910FF1671EC5D10BEA4C6C27282`, not the current binary: seven original-capture comparisons measured 2383.282 / 2248.601 ms (5.651% lower, 7/7 wins), identical digest and 33,060,350 native block pairs. No spread timing or gameplay FPS claim yet. The reused `ps2x_tests.flag-pack-base.exe` comparator now contains accepted `110ed3a`, SHA-256 `AE4D88555CD487A736551DC92B9A51D4F037084B59A97EF52F864FF15F029931`; older comparator identities below are historical. No extra executable slot, screenshot, or clone was added.
- Accepted FMAC producer change: PS2Recomp `110ed3a` replaces scalar flag-bit assembly with a shared portable 4x4 bit transpose and directly clears new flag entries. All 129 VU tests pass, including 1,048,576 canonical packing/mask combinations plus 4,096 high-bit cases, and both gameplay captures remain exact at normal and 1/8/16/64-cycle slicing. Seven alternating comparisons win 7/7 on each workload: original 2462.786 / 2394.965 ms (2.754% lower), spread 2098.631 / 2012.671 ms (4.096% lower). This is a modest offline VU gain, not a new gameplay FPS result. Test SHA-256 `AE4D88555CD487A736551DC92B9A51D4F037084B59A97EF52F864FF15F029931`. Gameplay executables have not been relinked.
- Upstream contribution submitted as [PR #250](https://github.com/ran-j/PS2Recomp/pull/250), commit `3e50ad9`, in the existing `C:/Programming/GitHub/PS2Recomp` checkout on `codex/vu-fmac-flag-packing`, based directly on upstream main `14b1e5c`. Its full Release suite passes 426/426. It contains only the generic helper/core update and exhaustive test, with no native game code/captures and no dependency on PR #245. The existing occupancy-mask branch is retained; no clone/worktree was added.
- Housekeeping after the FMAC contribution: all owned builds/tests exited; no game was launched. Primary/staged/candidate hashes remain unchanged. OpenXML1 is 7.805 GiB / 29,795 files. No new screenshots or executable slots were added. Previously policy-protected cleanup targets remain untouched. Next: pursue larger compiled execution savings; do not run a full boot just to time this small change. The current source and candidate differ only by verified work documented here, not an accepted gameplay-speed result.
- Rejected final-deadline publication: compiling one exit readiness store per touched VF/ACC/VI field passes 128 tests and original exact replay, but seven original comparisons measured 2909.037 / 2906.008 ms (0.104% lower, 4/7 wins). Removed completely before the flag producer change. Experimental test SHA-256 `DEDA52ECA6566505845A858C8A2B5A4FFF5D543BF1016808EC0F769A6B2EDFB7`.
- Fresh profiling on that rejected deadline variant: 2048 original repeats, 415 execution samples / 53 external / 362 in-module, zero drops/failures, matching map/image timestamp `0x6a9cb4f9`. The largest single symbol is native destination-14 `updateFmacFlagsFor` (34 samples, 9.39%); disassembly confirmed dozens of packing operations and a 40-byte stack zero/copy. New producer assembly removes the temporary, but MSVC keeps `packFmac` as an out-of-line helper in the inspected specialization. Logs in fixed `execution-profile.*` refer to the old experimental binary, not the current map. The initial 4096-repeat profile exceeded the replay cycle budget; the valid replacement uses 2048 without changing that limit.
- Schedule-cache experiment rejected: one thread-local exact slot plan per compiled block preserved the 128-test suite and original replay, but seven alternating 1024-repeat runs measured 3274.625 ms baseline / 3257.355 ms candidate (0.527% lower, 4/7 wins). This is effectively flat, not a useful gameplay improvement. Cache removed; do not repeat it unchanged. Experimental test SHA-256 `09B37A0822E9F0B97475EF86DA83C123CFB7DF5EC760293B3C7E49A1E9CD0BCE`; fixed comparison report overwritten in place. No spread speed comparison or live game run was warranted.
- Retained regression coverage expands mixed upper/lower incoming writes from 48 to 768 cases: all 64 three-pair prelude patterns across 12 budgets, with exact follow-up snapshots and compiled-block execution assertions. Restored runtime passes 128/128 VU tests and both captures at normal and 1/8/16/64-cycle slicing. Test SHA-256 `37407B2032245B81C44E84E4510F124F89E3BC85CBB48895E07AD792162DFE0E`. All three gameplay executable hashes below are unchanged; no new image, executable slot, or clone was created.
- Reference audit: Play!'s `ComputeSkipFlagsHints` can omit dead MAC pipeline updates, but `VUShared::TestSZFlags` still queues sticky flags independently. Its `GetStatus` has a TODO for additional flags. Do not treat this as permission to remove our complete FMAC/status work or as a drop-in exact backend. See the linked source audit in `VU-REPLAY.md`. The next larger compiled-path experiment needs to eliminate per-instruction runtime bookkeeping while preserving sticky status, pending writes and PATH1 timing, not cache the existing simulation.
- Latest gameplay candidate: `ps2EntryRunner.candidate.exe`, SHA-256 `EFCCF5749829741C8D1E0C500A9D8DE559FDE99BE57586C9367E2718FF9D8808`, 163,347,968 bytes, integrates PS2Recomp `1c6a874`. The guarded BelowNormal link exited 0 with the existing raylib duplicate-symbol and `/FORCE` warnings. Runner/PCH compile records have native pairs/blocks ON and native upper OFF; runtime object layouts are unchanged since `f801b9c`. Only replay helper declarations changed, so the existing `/O2`, runner-IPO-OFF objects were reused. PE32+/x64 validation passed. This replaces the former candidate slot, not primary/staged binaries.
- Live validation: the corrected phase and unprofiled runs both invoked the real New Game handler, loaded `maps/nyc/alison/nyc1_1_1.igb`, executed native blocks, reached vsync 1400 and exited 0. The phase run's native present-1280 image was inspected and shows textured New York/Wolverine with existing black-prop, foliage and HUD/effect defects. No rendering fix is claimed. Native block pairs across the full runs: 309,674,783 profiled / 307,828,823 unprofiled.
- Latest speed: 128 presents (1152-1280) took 41.2159952 seconds, 3.1055904 FPS, with performance profilers off. This is a single shared-host observation, not a controlled before/after against the earlier 3.9746 FPS sample, and it demonstrates no speed gain. The candidate stays opt-in. Reports/logs reuse `out/xmen-final3-build/gameplay-phase.*` and `gameplay-rate.*`.
- Current phase breakdown: 85 complete reports ending at ticks 1100-1397 on one thread cover 91,591.569 ms. Exclusive times: VU 59,140.185 ms (64.57%), GS 22,346.638 ms (24.40%), guest 5,025.790 ms (5.49%), transfers 4,181.379 ms (4.57%), events 525.912 ms, scheduler 313.320 ms, waits 0.112 ms. Profiling is not a frame-rate measurement. VU and drawing remain the high-leverage targets; waiting does not explain the slowdown.
- Initial attempted profile used the wrong `GameplayMissionNoMovie` preset without the required flags, remained black and executed zero native blocks. That result is excluded, not a new rendering regression or speedup. Use the new `run-gameplay-benchmark.ps1`: it pins `TitleGameplayFirst`, host clock, fast legal, branch-hook bypass, real New Game startup, native pairs/blocks, disabled host input and the vsync stop. It refuses unknown startup-archive edits, bounds logs, verifies workload markers, restores startup in `finally`, and never relaunches a closed window. Run `-PhaseProfile -CaptureFrame` for phase profiling, or `-CaptureFrame` for unprofiled frame timing. The latter completed with all workload gates true.
- Interactive opt-in: `run-interactive.ps1 -RuntimeVariant Candidate -StartupMovieMode TitleGameplayFirst` enables the compiled VU paths with normal user controls and no test timer. Default Auto/staged selection is unchanged. Launcher syntax passes; interactive host-input testing was not performed in this turn.
- Checkpoint housekeeping: both valid gameplay runs are closed and all four startup archive entries were checked against the retail originals after restoration. Primary/staged hashes are unchanged. Workspace measured 7.800 GiB / 29,757 files. No extra executable slot or clone was added. The runtime still emits its fixed legacy boot captures, plus the requested frame; cleanup remains pending the earlier policy rejection. Other MSBuild workers appeared after this task's link completed and were left untouched; these samples were not isolated-host benchmarks, and no causal claim about that background work is made.
- Next work: investigate a coarser compiled-VU execution path, using mature emulator implementations as references and retaining exact replay as the regression oracle. VU is still roughly two thirds of frame time after the small optimizations. Do not repeat rejected scalar arithmetic/flag-queue tweaks or blindly expand the block budget. No new upstream PR appeared beyond #249 in the current listing; no obvious JIT/hardware-renderer proposal appeared in open PR titles. That title scan is not an audit of every fork.

### Earlier Checkpoints

- Latest verified source: PS2Recomp `1c6a874` fixes overlapping native VF write retirement. The `40fb83f` boundary fixture reproduced the SAME failure with the arithmetic shortcut removed (sample 0, PC `0x3500`, budget 8, serialized offset 53930). Native blocks had exposed non-tail results early even when subsequent writes canceled them. The compile-time schedule now queues results when the same register is written again before readiness, including disjoint lanes needed for exact pending-write payloads. The interpreter and independent-write fast path are unchanged.
- Corrected baseline passes 128/128 VU tests: 1,608 arithmetic boundary/random cases and 48 mixed upper/lower write, incoming-pipeline and budget cases. The mixed fixture verifies block execution for sufficient budgets and checks that its prelude actually reaches the block with pending values. Both 32-record gameplay captures match exact state, memory, graphics packets and cycle counts at normal and 1/8/16/64-cycle slicing. Original digest `75d4ff1e67bbbc4c`, spread `6c13c7a10069aeef`.
- Final restored test build: SHA-256 `89B67AFE7ED1DA5F52C8D6B077E9BA610673DB82A47432DAB2DEE70B128C77EE`, 128/128 tests and both captures at every slice size above pass again after removing the shortcut. All build sessions exited 0. Primary/staged/gameplay-candidate hashes remain unchanged; no compiler, test or game process remains running. No new gameplay performance measurement was made.
- Rejected interior-normal ADD/SUB/MUL shortcut: with retirement corrected, it passes all 128 tests and both sliced captures, but seven alternating 1024-repeat original replays measure 2723.693 ms baseline / 2768.473 ms candidate (1.644% slower, only 1/7 wins). No spread performance run was needed to reject the original-workload regression. Shortcut removed again; retain the regression fixtures. Comparison report is the existing ignored `disc/vu-block-comparison.json`; candidate hash `B25C64B6A453ADDA5B04F7AB65C7F216AD8DC99A27EBF7B184980933BFD6CDDA` is rejected, not the current accepted source.
- Current cache: native upper OFF, native pairs/blocks ON, 64 private pairs / 16 private blocks plus eight public blocks (24 total, 25 source/dispatch files). Existing `ps2x_tests.flag-pack-base.exe` now contains the corrected `1c6a874` baseline, SHA-256 `802C86B1F6D21BB4C7BE082C5A525F2CEF9404D91B8BB28C4BE01058ED73EBD4`. It was overwritten in place, not added as another baseline file. Pre-ILW fixed baseline is unchanged. Workspace measured 7.790 GiB / 29,731 files; no new captures, game executables or clones were created. Cleanup remains pending the earlier policy rejection.
- Next performance work must target a larger measured cost. Do not repeat the rejected arithmetic guard or small flag-queue rewrites. Reassess warm whole-game frame costs and integration of the verified block work; check runner compile definitions and ABI consistency before linking a current gameplay candidate. Offline VU parity alone does not establish practical playability or a gameplay FPS gain.
- Primary and gameplay-candidate executables were rehashed and remain unchanged: `90D374181A33AFB9E872E44E7D5140E0685E4787AB654D33AB18EF5D56220105` / `42FFBDA55226B7A55AE07311A9BC35049165ADC6EA0DF46636B53BB49020108C`. Last live speed remains approximately 3.97 FPS; no improvement is claimed from this unfinished experiment.
- Earlier checkpoint: `eef46ed` directly clears retired integer/bool flag entries, avoiding an MSVC stack temporary in interpreter/native retirement. Test SHA-256 `BE50B67C2A9F55DF6C7D92ADA6F7D02CD0EA0B611F0559B1F84908FB3F99E267`; 126/126 focused tests and both captures exact at normal and 1/8/16/64 slicing. Seven-round spread replay improves 4.652% (2135.250 / 2035.908 ms, 7/7 wins); two original runs improve only 0.880% and 1.225% with 4/7 wins each and substantial host variation. No new gameplay FPS claim; game executables unchanged.
- Contributed the generic clear in `c5d12f8` to existing upstream PR #245; its upstream-based Release suite passes 427/427. Reused `C:/Programming/GitHub/PS2Recomp` and the existing comparison executable, with no additional clone, worktree, or baseline copy.
- Arithmetic normalization experiment from `40fb83f` is now rejected on measured speed after correcting the independent write-retirement failure. The retained boundary tests remain useful; the shortcut itself is removed.
- Rejected fixed deadline-mask scan: 127 tests and both sliced workloads passed, but original replay medians were 2401.464 / 2401.870 ms (effectively tied). The helper and its test were removed. Cleanup is still pending after the prior execution-policy rejection; no deletion is claimed this turn.
- Final restored build: `b024e1a`, 126/126 VU tests and both captures exact at normal and 1/8/16/64-cycle slicing. Test executable SHA-256 `F7C221FF0EF41A0E9F6137F05E3B872DC7F6B5F670720973FBFC8985AFB31B61`. Primary/staged/gameplay-candidate hashes remain unchanged. Cleanup command was rejected by execution policy before launch; the 5,780,480-byte `ps2x_tests.flag-pack-base.exe` and inactive Debug directories remain pending authorized cleanup. Do not claim they were removed.
- Execution-only profiling checkpoint: PS2Recomp `b024e1a` excludes cold passes and snapshot setup/comparison from sampled addresses. The baseline passes 126/126 VU tests, exact original/spread replay, and reporter scope/identity tests. Warm in-module samples put native flag retirement at 11.49% / 13.59% and general pipeline retirement at 10.34% / 16.02%. These are coarse instrumented VU-only profiles, not game-frame percentages.
- Rejected MAC lookup-table experiment: exact at normal and 1/8/16/64-cycle slicing, but seven-round medians improve only 0.818% original and 0.522% spread. Removed rather than promoting an effectively flat optimization. Game executables remain unchanged. Remove the finished `ps2x_tests.flag-pack-base.exe` comparison file after rebuilding the restored source.
- Flag-retirement investigation led to direct clearing above. Native lower operations currently admitted do not read MAC/status/clip; that alone is not sufficient proof that batching is safe. Do not repeat the rejected completion-index, preassigned-retirement, or fixed-mask implementations unchanged. Practical FPS remains unmet.
- Post-specialization cleanup: removed the finished comparison binary and 187 inactive Debug directories. Workspace is 7.739 GiB / 29,166 files; all eight protected artifacts, the fixed baseline, and 22 active block-source/dispatch files remain. Primary/staged/gameplay-candidate hashes are unchanged, and no owned test/build/game processes remain.
- Current experimental source: `2ddfc04` makes VF-result deferral a compile-time decision using each block's exact schedule. Build passed; 126/126 VU tests and both captures at normal and 1/8/16/64-cycle slicing remain exact. Test SHA-256 `1737D0CF99186FF812E8BF58A8BF3F342CDBB162723371C499F7FFCCE5E3A102`, 5,778,944 bytes. Same-selection seven-round medians improve 1.343% original (2429.307 / 2396.670 ms, 5/7 wins) and 0.513% spread (2006.897 / 1996.594 ms, 4/7 wins). Spread is effectively flat; keep the smaller code without claiming a meaningful FPS gain. The game candidate is unchanged.
- Execution-only sampling is now implemented in `b024e1a` above. The older profile's largest symbol was test serialization, not game execution. Preserve the fixed accepted baseline. Do not spend another full boot solely to time these small changes or repeat previously rejected queue-index/stack-materialization experiments without new evidence.
- Coverage selector checkpoint: PS2Recomp `5bf8813` is pushed. Active local cache selects `coverage` and `disc/vu-native-pairs-weighted.inc` (70,152 bytes, 426 exact PC weights), with unchanged 64-pair / 16-private-block limits and five public blocks. Source default stays `frequency`. Standalone selector tests: eight selection / seven rejection checks pass. VU suite: 126/126, both captures exact at normal and 1/8/16/64 slicing. Candidate test hash `415DBAFA25E21084FF1FF9F728FD556400BEBB8C07AEE91DFEA603E780E5D317`.
- Combined coverage/ILW build beats the fixed pre-ILW baseline by 2.763% on original (2562.666 / 2491.862 ms, 7/7 wins) and 1.576% on spread (2098.022 / 2064.947 ms, 6/7 wins) over seven alternating rounds. Actual block pairs increase to 64,508 / 19,096, versus baseline 59,864 / 17,978. This is modest offline progress, not a gameplay FPS claim. The game executable is unchanged.
- Latest cleanup removed 187 inactive Debug directories. Workspace is 7.735 GiB / 29,131 files, with all eight protected artifacts, the one active comparison baseline, and 22 native-block source/dispatch files verified. No owned build, test, or game process remains running.
- Latest experimental source: PS2Recomp `28a40cf` supports four-cycle ILW results in native blocks, including delayed tail writes, overwritten results, and VI queue-capacity guards. All 126 VU-related tests pass, including 198 new synthetic cases. Both captures remain exact at normal and 1/8/16/64-cycle slicing. Test executable SHA-256 `CE2728D5207DE273D814BBEA61A38A7A86ECC567F52DDDCB6DC91BB67AE1721A`, 5,705,728 bytes. Current cache: original recipe, 64 private pairs / 16 private blocks plus five public blocks (21 blocks total).
- Do not promote `28a40cf` to a game build yet. Seven alternating comparisons against `1d14571` measure original 2502.122 / 2582.278 ms (3.204% slower, 1/7 wins) and spread 2139.856 / 2125.355 ms (0.678% lower, 4/7 wins). Shared-host variation is visible. Coverage changes from 59,864 to 59,726 pairs on original and 17,978 to 18,104 on spread. The new eligible 23-pair block at 0x18a0 displaces 0x0b80 under the unchanged budget; capability alone does not improve the current selection.
- Keep the single fixed comparison baseline `ps2x_tests.accepted-blocks.exe`, SHA-256 `A81E93D0867C631B145E98C996446DDBEDD5693BBF64786602BE040C52DF7A76`. This is the existing pre-ILW binary renamed, not another copy or game EXE backup. Compare future selection work against it and replace/remove only when that experiment is resolved.
- Measured fallback audit: every canExecuteFast refusal in both recorded workloads was a short budget. There were zero refusals for pending ACC/store/divide/EFU operations, branches, PATH1 boundaries, entry readiness, or VF queue slots. The temporary per-block counters and diagnostic test-exit change were removed. Do not relax these pipeline gates on speculation. This audit covers the selected block entry path, not missing lookup targets or code-match failures.
- Selection experiment completed in `5bf8813` as recorded above: exact per-PC weights and an entry-capped marginal coverage estimate improve both offline workloads. Runtime cost is not part of this estimator. Keep the limit at 16 and preserve both captures; do not infer execution weights from rank or blindly increase the compiler budget. The main 0x0a88 loop stops at its branch/delay slot, not at the following ILW.
- Cleanup after the timing audit removed both temporary comparison executables, 186 inactive Debug directories, and two other disposable files. Workspace: 7.710 GiB / 29,090 files. All eight protected artifacts and all 21 current native-block source/dispatch files remain. Primary, staged, and gameplay-candidate hashes are unchanged; no owned build/test/game processes remain running.
- Stop checkpoint: PS2Recomp `1d14571` schedules internal instruction waits at compile time instead of rejecting those native blocks. The recovered guarded `ps2x_tests` build exited 0. All 125 VU-related tests pass, including 78 internal-wait/budget/PATH1 cases. Both 32-record gameplay captures match exact state, memory, GIF output, cycle counts, and digests at normal and 1/8/16/64-cycle slicing. Test executable SHA-256: `7F4AC65C4B338F2131AD97DA6DEBEAF3DFDCAEF016BE3D39767436DDC55686F6`.
- Scheduler timing complete: with the original recipe, seven alternating rounds measure 2443.675 / 2420.845 ms on the original workload (0.934% lower, 5/7 wins), and 2022.707 / 2030.595 ms on the spread workload (0.390% slower, 2/7 wins). Coverage is identical before/after. This is effectively neutral, not a demonstrated gameplay improvement. The scheduler remains experimental in source; do not spend a game probe or relink on it alone. Temporary `block16` and `schedule-base` comparison binaries are now eligible for cleanup.
- Active test build restored to `disc/vu-native-pairs.inc`, 64 private pairs / 16 private blocks plus four public synthetic blocks. Build exit 0; VU suite 125/125; both captures exact at normal and 1/8/16/64-cycle slicing. Test SHA-256 `A81E93D0867C631B145E98C996446DDBEDD5693BBF64786602BE040C52DF7A76`. The game candidate remains unchanged at `42FFBDA5...` and does not contain scheduler checkpoint `1d14571`.
- Broader recipe plus scheduler lost all seven rounds against the accepted original selection on both workloads: 8.192% slower on original and 2.783% slower on spread. Do not switch back on the assumption that broader capture coverage implies better kernel selection. Keep both private captures as correctness gates.
- Broader selection alone was not an improvement: 3 alternating 2048-repeat comparisons measured median 2062.878 ms original recipe / 2109.155 ms broader recipe (2.243% slower, zero wins). Both captures use byte-identical code images, but different exercised PCs. Preserve both captures and original recipe. Do not promote the broader selection on coverage assumptions.
- Latest accepted VU optimization: PS2Recomp `f801b9c` precomputes block-entry register deadlines. All 123 then-current VU tests pass, including 270 new deadline cases, and the original 32 captures remain exact with 1/8/16/64-cycle resumption. Seven alternating 1024-repeat comparisons against `7e2f840` measure 2631.679 ms before / 2432.532 ms after at the median (7.567% less VU replay time, seven wins). Block coverage remains 59,864 pairs at one repeat. The current game candidate includes this change and recorder checkpoint `6e29441`.
- Capture coverage audit: every original `disc/vu-replay.bin` record comes from tick 1100 and the same code image. Recorder checkpoint `6e29441` now spreads capture quotas. Probe 2275 completed 32 interpreted-reference samples across ticks 1100-1160 in the separate 1,907,530-byte `disc/vu-replay-spread.bin`. Both captures are retained; both remain exact with 1/8/16/64-cycle resumption. The current VU-related suite passes 124/124.
- Prior profile context: a 4096-repeat spread profile has 299 in-module samples: pipeline retirement 9.03%, native flag retirement 6.02%, interpreter upper dispatch 5.69%, queued stores 5.02%, and readiness calculation 3.34%. These are coarse sampling leads and include replay overhead, not whole-game percentages. The fallback audit above now rules out pending-pipeline refusals in these selected blocks. Do not blindly expand the block budget; the earlier 32-block trial also regressed.
- The entry-deadline change also wins five of seven alternating 2048-repeat comparisons on the broader capture: median 2195.355 ms before / 2064.303 ms after, a 5.970% replay reduction with late shared-host variation. New capture identity: 16,338 cycles per repeat, digest `6c13c7a10069aeef`. This is not a gameplay FPS claim.
- Current candidate: `ps2EntryRunner.candidate.exe`, SHA-256 `42FFBDA55226B7A55AE07311A9BC35049165ADC6EA0DF46636B53BB49020108C`, 163,238,400 bytes. Its guarded relink completed under the unchanged BelowNormal / affinity 0xF / one-worker / 2048 MiB limits with the existing raylib duplicate-symbol warnings. PE validation passed. Probe 2275 captured interpreted reference work and exited normally at tick 1200. Probe 2276 ran compiled blocks with capture/profilers off, rendered New York and Wolverine in its native present-1280 framebuffer, and exited normally at tick 1400. The existing black-prop, missing-foliage, HUD, and effect defects remain. Present indices 1152-1280 took 32.2047 seconds (3.9746 FPS). This single shared-machine sample is not a controlled before/after or a sustained-speed guarantee. Owned game/observer processes are closed and startup scripts restored.
- The complete 107-unit runner build uses `PS2X_ENABLE_RUNNER_IPO=OFF` with `/O2`, while runtime/native-kernel IPO remains ON. Earlier Probes 2271/2272 used candidate `CBA24C9E8B38ABBC6D17B5769FF441812E0746EED5B5317D1DD82091E9028119` and measured 3.4720 FPS with blocks / 3.3964 interpreted over the same 128-present window. That remains the latest same-executable native/interpreted comparison; the new single-mode sample does not supersede it as a controlled comparison.
- The failed link left `ps2EntryRunner.profile.exe` incomplete at 2 MiB. Deletion was policy-blocked; leave it alone and do not launch it or retry removal/moving. The new fixed `Candidate` slot avoids this artifact without adding a per-probe executable backup. Primary/staged hashes remain unchanged.
- Keep SFD playback out of scope. The target remains basic first-level playability.
- Preserve the known-good primary runtime at `PS2Recomp/out/xmen-final3-build/ps2xRuntime/Release/ps2EntryRunner.exe`.
- The environment-cache change retains the original dispatch gate and checkpoint batching. The unconditional early-dispatch experiment was removed. Current candidate runs reach rendered gameplay; no standalone frame-rate gain is claimed for caching.
- Probe 2251 re-established textured first-level rendering with the known-good primary runtime, `TitleGameplayFirst`, host clock, fast legal handling, aggressive branch-hook bypass, and full CPU rasterization. `probe2251-present1099.png` is the current gameplay evidence.
- User testing confirmed that Wolverine can move and basic combat works in the first level, although gameplay is currently extremely slow.
- Do not use `PS2X_SKIP_CPU_RASTER_BEFORE_PRESENT` for correctness or visual validation with host-clock pacing. Probe 2248 reached the NYC package quickly but later entered an invalid guest state and rendered black.
- Previous selected-source performance conclusions are invalidated: the helper compiled objects without rebuilding `ps2_runtime.lib`, so candidate executables could still contain old code. Do not conclude that environment caching failed or checkpoint batching caused a fault from those runs.
- Fixed the helper to run `_Lib` without `SelectedFiles` after a successful static-library source compile. Ordinary and unity-build regression tests prove candidate behavior changes, untouched library code remains available, and the primary executable is preserved.
- Probe 2252 reproduced full-rendering first-level progression to present 1140 with the unchanged primary. Across presents 1000-1140, CPU rasterization averaged 94.57 ms (44.60 ms triangle lists, 46.35 ms strips), about 23,424 submissions per frame. This is raster time, not total frame time.
- Probe 2253 tested the verified rebuilt candidate with the usual bypass enabled. Its present-1140 native capture confirms textured New York and Wolverine with the existing black props/HUD defects. The corrupted image at present 768 was still loading, not a permanent world-rendering regression. First batches over 20,000 submissions occurred at present 964 in Probe 2252 and 1010 in Probe 2253; do not compare an arbitrary earlier present as if loading had finished.
- Probe 2254 tested the broader early dispatcher with per-branch checkpoints, reached present 1140, and closed normally under the guard. This experiment was not retained. Concurrent test/build work makes its elapsed time unsuitable for a clean performance comparison. No practical frame-rate improvement is established.
- Focused tests after restoring the original dispatch control flow: VU 91/91; GS 76/79; dispatch 2/5. The three dispatch failures reproduce with and without the early-path change: indirect-call registration/return and missing-target tests encounter existing non-code-call filters. The three GS failures concern CSR/IMR access and two T4 triangle sampling cases. The full suite was interrupted; do not report a full-suite pass.
- Probes 2255/2256 compare early depth rejection off/on in the same executable. Across presents 1100-1400, mean CPU raster time fell from 98.98 ms to 63.03 ms (36%). This is not total frame time. A 20-second gameplay sample with the GS profiler enabled still measured only about 2.7 FPS.
- Probe 2257 replayed all 23,042 triangle batches in gameplay present 1200 against private copies of their starting VRAM and CLUT, comparing all 4 MiB after every draw: zero mismatches. Earlier separate-run screenshots differ in dynamic effects/HUD state; they are not deterministic frame comparisons.
- Early-depth synthetic tests compare all VRAM bytes across 128 cases: identical on/off digest `e5a1e2f16d5d1336` in the bring-up branch. Corrected two T4 fixtures to request CLUT loading (CLD=1), without changing expected pixels. Current local GS suite: 80/81, with only the existing CSR access failure. VU: 91/91. Profiling helper: 2/2. The broader local kernel suite did not finish and was stopped; it is not a passing result.
- Submitted focused upstream PR #246, `https://github.com/ran-j/PS2Recomp/pull/246`, from the existing clean checkout. Upstream-main-based Release suite: 427/427. Both complete-suite runs match digest `e1af4f1cfc948cc3`; the digest differs from the bring-up branch because the rendering implementations differ.

## Performance And Correctness

- Prioritize VU execution cost. Probe 2258's 80 complete timing windows between gameplay presents 1101 and 1299 cover 88,161 ms: VU exclusive 65,177 ms (73.93%), GS submission/drawing 16,195 ms (18.37%), other guest execution 3,181 ms (3.61%), transfers excluding VU/GS 2,801 ms (3.18%), scheduler 218 ms, events 433 ms, and waits 0.1 ms. These are opt-in instrumented wall-time measurements, not a new frame-rate claim.
- Probe 2274 reprofiled the current native-block candidate after first-level rendering stabilized. Its 77 complete windows from ticks 1100-1398 cover 81,294.961 ms: VU exclusive 54,137.980 ms (66.59%), GS submission/drawing 19,077.840 ms (23.47%), other guest execution 3,804.650 ms (4.68%), transfers excluding VU/GS 3,349.980 ms (4.12%), events 472.690 ms, scheduler 281.640 ms, and waits 0.080 ms. The run reached textured gameplay, exited normally at tick 1400, and confirms that VU execution remains the primary performance target with GS a clear secondary cost. Profiling overhead means this is a cost breakdown, not a new FPS measurement.
- Probe 2259 completed the sampled VU diagnostic through present 1200. Its 39 gameplay timing windows cover 44,364 ms. Among named sampled instruction stages, upper arithmetic and pipeline retirement are the largest. Do not treat the remaining outer-slice time as pure interpreter work: it includes instrumentation overhead, which is material for these very short helpers.
- Detailed VU instrumentation now requires the CMake option `PS2X_ENABLE_VU_DETAIL_PROFILE=ON` plus runtime variables `PS2X_RUNTIME_PHASE_PROFILE=1` and `PS2X_VU_DETAIL_PROFILE=1`. It samples 1/64 complete VU slices after tick 1000. Keep the CMake option OFF in ordinary gameplay builds so per-instruction profiling branches are absent.
- Operand-normalization inlining and an upper-NOP fast return passed all 93 local VU tests. Nine arithmetic-workload runs before/after retain digest `45e6d3166964d75e`; median execution time is 21.103 ms before and 19.971 ms after on this machine (about 5%). This is a synthetic workload, not a gameplay FPS measurement.
- Probe 2260 reached present 1200 and its native framebuffer confirms textured New York/Wolverine with the existing black-prop, HUD, and red-circle defects. It closed under the guard and restored the startup archive. Comparable complete diagnostic windows averaged 441.09 ms/frame before (2259, 98 frames) and 382.26 ms/frame after (2260, 96 frames), still about 2-3 FPS. These shared-machine, instrumented runs are not an isolated or sustained frame-rate guarantee.
- Submitted upstream PR #247, `https://github.com/ran-j/PS2Recomp/pull/247`, from commit `cf1efac` in the existing clean checkout. Both upstream-main-based test runs pass 427/427 with arithmetic digest `3308d7380e3bfe64`. The runtime change has no dependency on the occupancy-mask PR or X-Men-specific code. Bring-up commit `8cbe200` is pushed separately.
- The ordinary build with detailed profiling compiled out also passes 93/93 VU tests and preserves digest `45e6d3166964d75e`. Probe 2261 reached present 1280 with normal textured first-level output and existing lighting/HUD/effect defects. Neither phase nor raster profiler was enabled. External observation of native present-log entries 1152 to 1280 measured 128 frames in 46.576 seconds, approximately 2.748 FPS. This does not establish a meaningful overall improvement over the earlier roughly 2.7 FPS sample.
- Probe 2261's guard closed the runtime and restored the startup archive. Its tested ordinary binary is staged as `ps2EntryRunner.next.exe`; `probe2261-present1280.png` is the current native framebuffer checkpoint. The task remains active and practical interactive speed is not achieved.
- Probe 2258 reached present 1300, its native framebuffer shows textured New York and Wolverine with known black-prop/HUD/effect defects, and the guard closed the runtime and restored the startup archive. `probe2258-present1300.png` is the current checkpoint capture.
- Use `PS2X_RUNTIME_PHASE_PROFILE=1` for phase timings (`calls/inclusive-ms/exclusive-ms` per stage), `PS2X_GS_CPU_PROFILE=1` for raster-only timings, and `PS2X_GS_VERIFY_EARLY_DEPTH_PRESENT=<present>` for one-frame same-state triangle verification. Leave these disabled for ordinary play. The phase profiler reports only completed outer scopes and subtracts nested work.
- Added bounded process-local VU snapshot/replay in bring-up commit `eaee632`. Probe 2262 captured 32 real slices in 1,914,088 bytes; 4096 timed executions reproduce every register/pipeline field, all VU data bytes, and emitted GIF bytes/cycles, digest `75d4ff1e67bbbc4c`. See `VU-REPLAY.md`; the capture remains private in ignored `disc/vu-replay.bin`.
- Removed redundant same-cycle pipeline retirement scans and optimized contiguous PATH1 qword copying. Local VU tests pass 97/97, including boundary and zero/one-cycle resume checks. Retirement-only replay medians are 589.740 ms before and 564.147 ms after over nine interleaved pairs (about 4.3%). Packet-copy incremental timings were noisy; no independent gain or whole-game FPS improvement is claimed.
- Submitted upstream PR #248, `https://github.com/ran-j/PS2Recomp/pull/248`, commit `ebd1d34`. Clean upstream main passes 425/425 tests and the PR passes 426/426. It does not depend on earlier VU PRs or the replay diagnostics.
- Submitted upstream PR #249, `https://github.com/ran-j/PS2Recomp/pull/249`, from clean upstream-main-based commit `baa0f56`. The default-on runner IPO toggle keeps ordinary behavior unchanged while allowing only `ps2EntryRunner` to omit `/GL`, `/LTCG`, and CMake IPO. MSVC Release configuration checks with global IPO enabled prove the OFF runner omits both settings while `ps2_runtime` retains IPO, and ON restores runner IPO/LTCG. The temporary source-only worktree and configure output were removed after push.
- Probe 2263 reached present 1280 with textured New York and Wolverine and the known black-prop, missing-foliage, and HUD defects. Native capture: `probe2263-present1280.png`. Replay and both performance profilers were off; detailed profiling was compiled out. External log observation measured 128 frames from 1152 to 1280 in 41.0026 seconds, or 3.1218 FPS. This is better than the earlier 2.748 FPS sample, but not an isolated or sustained-speed guarantee. The guard closed the runtime and restored the startup archive; the observer is also closed.
- Implemented sparse VI-mask iteration in bring-up commit `746e884` and PR #248 follow-up `a64d924`. All 256 pairs of VI source registers cover VI0, high registers, every destination, repeated loads, final values, stalls, and the end-bit delay slot. Local VU tests pass 98/98; the clean upstream PR passes 427/427.
- The longer VI-mask replay comparison alternates execution order across nine pairs with 512 repeats per case. Median time falls from 2299.731 ms to 1981.324 ms (13.85%), with unchanged digest `75d4ff1e67bbbc4c` and every candidate run faster than every baseline run in that batch. Ordinary Probe 2264 reaches present 1280 with textured New York/Wolverine and existing visual defects, measuring 3.2485 FPS over 128 frames in 39.4022 seconds. This is not an isolated or sustained-speed guarantee. Runtime and observer are closed; the startup archive is restored.
- Added an opt-in, offline-only native sampler to the replay test, plus a build-identity-checked MSVC-map report. Confirmation: 895 samples, no drops/failures, 65,536 timed replay slices with identical results. Among the 776 in-module samples, retirement is 22.29%, VF write queuing 9.28%, FMAC flag queuing 8.63%, and the main VU loop 12.76%. Sampling includes replay restoration/comparison and must not be used as an ordinary FPS benchmark. The sampler touches only its own test thread and always resumes before allocating/logging.
- Rejected a completion-cycle index for pending flags/VF writes on September 4. It passed all 100 focused VU tests and exact real-game replay, but nine 512-repeat pairs had medians 2148.903 ms baseline / 2131.059 ms candidate, while seven longer 2048-repeat pairs had medians 7873.528 / 7956.781 ms. The inconsistent timing does not justify the added derived state. The index and replay reconstruction changes were removed; slot-reuse, single-cycle, and restart/reset regression tests are retained. No gameplay speed gain is claimed from this experiment.
- Rejected returning cached decoded instruction pairs by reference. All 119 VU-related tests and the real-game replay passed, but nine alternating 1024-repeat pairs had medians 3923.547 ms baseline / 4080.658 ms candidate (about 4% slower). The runtime change was removed; the new tracked/untracked, misaligned-PC, nullable-memory, and repeated-code-edit regression is retained. No new game probe or runtime replacement is justified by either rejected experiment.
- Restored production code passes 101/101 focused vector tests and 119/119 VU-related tests; 4096 real replay executions retain digest `75d4ff1e67bbbc4c`. Bring-up regression commits `15dbbda` and `d65a574` are pushed. PR #245 now includes the slot-reuse and cancellation tests in `af56130`, with 427/427 clean upstream-based Release tests. The existing clean checkout is on `codex/vu-pipeline-occupancy-masks`; no new clone was created. The PR body no longer treats an earlier small title-screen timing sample as a current gameplay-speed result.
- Next major performance task: prototype compiled VU execution in offline replay, with an interpreter fallback. Preserve exact cycle-budget returns, pending arithmetic behavior, code-generation invalidation, and PATH1 bytes/emission cycles before integration. Inspect the existing PCSX2 microVU/emitter interfaces for reuse, but do not import its block overshoot or global emulator dependencies unchanged. If additional clean-entry captures are required, add them without weakening the existing mid-slice oracle. Move beyond per-instruction bookkeeping micro-optimizations.
- Native-upper preparation: the shared-arithmetic extraction passes all 119 VU-related tests. A one-cycle cold replay identifies 105 upper-instruction candidates while preserving all 32 captured results and digest `75d4ff1e67bbbc4c`. Candidate fetch counts include dependency-stall retries; the generated recipe stays in ignored `disc/`.
- The opt-in Windows native-upper prototype now passes 122/122 VU-related tests, including 58 synthetic instruction kernels across 16 boundary-value patterns, ABI rejection/reload, provider switching, and fallback/cache invalidation. All 32 retail captures match exactly over 4096 timed slices, with 100% native upper-instruction coverage. The private DLL is 86 KiB. It is loaded only by tests, not by the game.
- Nine alternating native/interpreted comparisons at 1024 repeats retain digest `75d4ff1e67bbbc4c`: medians 4476.139 ms interpreted / 4164.376 ms native (6.965% less execution time). Native wins every pair, but this is a modest warm-replay gain, not a gameplay FPS result. Keep the prototype experimental. Next: specialize the still-generic exact-result/product-sticky helpers without forced-inlining blowup, then address compiled-block scheduling/retirement. Do not spend another boot probe merely to measure this small initial kernel gain.
- The current local test build is configured with `PS2X_ENABLE_VU_NATIVE_UPPER=ON`; the option defaults OFF in source. Native-enabled class layout differs. Do not relink a game executable from stale runner objects: reconfigure the intended mode and rebuild both runtime dependencies and runner translation units together before any candidate game link.
- Native prototype checkpoint `40a2193` is pushed to the PS2Recomp bring-up branch. The ordinary and unity build-helper fixtures both pass with the memory watchdog present. Native-enabled GS tests remain 80/81 with the same pre-existing CSR read/write failure; no full-suite or gameplay-speed improvement is claimed.
- Native flag specialization now passes 122/122 VU-related tests with 5536 synthetic arithmetic cases (173 words, 32 boundary/random patterns), including masks, in-place destinations, and VF0. The 32 retail captures still match exactly over 4096 timed slices, digest `75d4ff1e67bbbc4c`. Nine alternating 1024-repeat pairs have medians 5232.074 ms interpreted / 4632.082 ms native (11.468% less replay execution time), with native faster in each pair. Raw times across different batches are not an isolated before/after comparison.
- Completed the opt-in native loader validation in a separately linked game candidate, measuring the same executable with and without the provider. The primary and staged Probe 2264 executable are preserved. The loader is shared with the tests and rejects incompatible/relative module paths; unknown instruction words retain the interpreter fallback.
- Experimental runtime checkpoint `cbadbb1` is pushed. After loader integration, VU tests pass 122/122, the focused PS2GS suite remains 80/81 with the existing CSR failure, and 4096 native retail replays retain the exact digest. The consistent runner rebuild and live game validation are recorded below.
- The subsequent 107-step rebuild and separate profile-candidate link succeeded. Probes 2265/2266 both reached textured New York/Wolverine and exited normally at vsync 1400 with the startup scripts restored. Same-executable, profiler-free external observation of presents 1152-1280 measured native 36.7985431 seconds / 3.4784 FPS and interpreted 38.0110288 seconds / 3.3674 FPS. This small difference between two sequential shared-machine runs does not establish a meaningful sustained gain. Both native framebuffers retain black-prop, missing-foliage, HUD, and effect defects; these are not deterministic pixel comparisons.
- Native-run post-join VU1 counters are 1,550,972,856 native / 233,407,006 interpreted (86.92% native over the whole run); the interpreted run reports 0 / 1,290,629,286. VU0 remains 0/0. The game loader's negative missing/relative-module test exits 1 before window initialization. Keep the candidate experimental; do not replace the staged build for this small measured difference.
- Next performance work: prototype compiled instruction blocks in offline replay to eliminate more per-pair decode/dispatch and pipeline bookkeeping. Keep the interpreter fallback and prove exact 0/1/64-cycle resume boundaries, upper/lower operand hazards, delayed writes/flags, branch/end-bit behavior, microcode invalidation, and PATH1 payload/emission cycles. The current upper-only kernels leave the principal scheduling/retirement path interpreted. Do not resume tiny full-boot timing probes or visual polish while gameplay remains this slow.
- Added a bounded private pair-recipe exporter as the first compiled-block input. Its one-cycle cold pass counts only retired PCs, excluding dependency-stall retries, and writes exact PC/lower/upper triples plus replay entries and observed successors without state, memory, or GIF payloads. All 32 gameplay captures use the same 16 KiB code image and exercise 16 entries, 426 PCs, 439 edges (26 non-linear), 199 unique lower words, and 105 unique upper words. Focused PS2VU1 is 54/54; the retail replay remains 32/32, 40,803 cycles at one measured repeat, digest `75d4ff1e67bbbc4c`.
- The exporter now partitions straight-line coverage at entries, control-flow/missing edges, and a maximum eight-pair compiler unit: 80 blocks averaging 5.32 pairs, maximum eight. The ranked-word records expand the ignored manifest to 56,202 bytes. Duplicate PCs, invalid successors, unaligned/out-of-range addresses, zero counts, and unbounded limits are rejected.
- The opt-in exact-pair AOT prototype compiles 64 private pairs plus three public synthetic pairs. PS2VU1 passes 55/55; pair mode off/on both reproduce all 32 retail captures, 40,803 cycles at one repeat, and digest `75d4ff1e67bbbc4c`. The top-64 set handles 62,982/80,194 retired pairs (78.54%).
- The direct whole-block prototype now compiles 16 private replay-ranked blocks plus one public synthetic block. It handles 49,182/80,194 retired pairs (61.33%) inside 2,682 successful block entries, and all 32 captures retain exact state, VU memory, GIF output, 40,803 cycles, and digest `75d4ff1e67bbbc4c`. Seven alternating 1024-repeat comparisons measured 4,361.182 ms for exact-pair mode and 3,795.386 ms with blocks at the median, a 12.973% VU replay reduction. Public PS2VU1 is 53/53; replay-enabled PS2VU1 is 54/54. This is not yet a gameplay FPS result.
- A 32-private-block trial raised coverage to 53,170/80,194 pairs (66.30%) but was 9.836% slower than 16 blocks in a direct seven-round alternating comparison (3,580.251 vs 3,259.639 ms median). Keep the active private limit at 16; the larger generated set and comparison binary were removed.
- Native FMAC flag packing now specializes the constant destination mask while preserving the generic dynamic interpreter path. Public PS2VU1 passes 53/53, and all 32 gameplay captures retain 40,803 cycles and digest `75d4ff1e67bbbc4c`. Seven alternating 1024-repeat comparisons against the pre-change 16-block binary measured 3,925.071 ms baseline / 3,643.335 ms specialized at the median, a 7.178% replay reduction. PS2Recomp checkpoint `4d5aea2` is pushed; do not spend a gameplay probe on this offline-only gain.
- Native block readiness now expands constant instruction metadata through an ordered template fold while retaining live pipeline, deadline, slot, and cycle-budget checks. Public PS2VU1 remains 53/53 and the 32 captures remain exact. Seven alternating 1024-repeat comparisons measured 3,266.214 ms baseline / 3,167.319 ms specialized at the median, a 3.028% reduction, with the candidate winning six pairs and adding about 31 KiB. PS2Recomp checkpoint `6d235de` is pushed; continue toward larger block scheduling savings before another gameplay probe.
- Rejected direct retirement through preassigned FMAC flag slots. The candidate remained exact over all 32 gameplay captures at 40,803 cycles and digest `75d4ff1e67bbbc4c`, but seven alternating 1024-repeat comparisons measured 3,713.223 ms accepted / 3,952.895 ms candidate at the median, making the candidate 6.455% slower. The code was removed. Use Play!'s precomputed block timing as architectural evidence for a larger block-exit materialization step, not another narrow hybrid around the generic queues.
- Rejected local-array block-exit materialization. Public PS2VU1 passed 53/53 and all 32 gameplay captures remained exact, but the candidate lost all seven alternating 1024-repeat rounds: 3,265.104 ms accepted / 3,453.661 ms candidate median, a 5.775% regression. The source was restored. Do not simulate architectural queues in per-call stack arrays; the next compiled path must generate direct state transitions with no runtime schedule construction or copying.
- Per-entry diagnostics found that active PATH1 transfers, rather than code overlap alone, explain much of the block fallback: 1,352 attempts across entries 0x18c8/0x1908/0x1948/0x1968/0x19a0 encountered active transfers. Native blocks now advance those transfers after architectural writes each cycle, while falling back before an unparsed GIFtag. Block coverage rises from 49,182 to 56,582 of 80,194 pairs (70.56%). Seven alternating 1024-repeat comparisons measured 3,258.914 ms baseline / 3,118.863 ms candidate median (4.297% reduction), with seven candidate wins and unchanged digest. Temporary diagnostic hooks were removed before the final correctness checks. The replay result does not establish gameplay FPS.
- Pending-VI entry support is accepted in `e793e41`. A temporary rejection trace after that change showed 2,174 remaining preparation refusals, all first-instruction upper-VF reads exactly one cycle early, plus 60 short-budget refusals. `7e2f840` absorbs that architectural stall inside the block. Seven alternating 1024-repeat comparisons measured 3,620.144 ms pair-only / 2,834.312 ms blocks at the median, a 21.707% reduction with seven block wins and unchanged digest. Repeated block coverage rose from 30,170,875 to 30,680,300 pairs. Diagnostics were removed before the final rebuild and validation.
- Nine alternating 128-repeat comparisons measure interpreter median/mean 501.040/501.156 ms and native-pair 465.445/468.259 ms, a 7.10% median reduction with the native path faster in every pair. This is exact offline replay, not a gameplay FPS result. PS2Recomp checkpoint `f14bdea` is pushed.
- Native-pair sampling places `commitReadyPipelines` at 25.20% of in-module samples, `run` at 17.07%, `queueVfWrite` at 10.16%, and `updateFmacFlags` at 8.54%. Specialized instruction bodies are no longer the main sampled cost. Next implementation step: reduce queue/retirement crossings with exact compiled scheduling while preserving mid-slice pending state, code invalidation, branches, cycle budgets, and PATH1 timing. Do not spend a full gameplay probe on the current 7.10% replay-only gain.
- Inspected PCSX2 microVU at local reference commit `d073d7501` (August 30). It precomputes pipeline timing while compiling blocks, but depends on global VU/EE state, GIF/VIF scheduling, MTVU, configuration, executable-memory management, and its emitter. It is not a drop-in library. A future compiled path must preserve this runtime's slice boundaries and PATH1 visibility rather than import PCSX2's block-level cycle accounting unchanged.
- Small interpreter savings alone are insufficient for practical playability. Use the replay to quantify larger execution-path changes, and evaluate a compiled VU path if interpreter improvements plateau. Do not turn this into an unbounded series of full-boot micro-optimization probes.
- Preserve floating-point edge cases, flags, pipeline timing, XGKICK order, and self-modifying microcode behavior while optimizing VU execution. The existing occupancy-mask optimization is already integrated.
- Measure dispatch/checkpoint costs before changing scheduler policy, but the new broad profile does not identify dispatch or waiting as the dominant gameplay cost.
- Investigate missing recompiled target `0x00504310`, reached by an indirect call at `0x001BD21C`. Probe 2251 continued and rendered, but this remains a correctness gap.
- Profile the runtime during actual first-level rendering, without thread suspension during correctness runs.
- Optimize the measured CPU triangle raster cost; preserve a full-rendering baseline for comparison.
- Improve sustained frame rate from the current single-digit range to practical interactive speed.
- Resolve the remaining CSR/IMR and three dispatch-test failures, and identify the local kernel-suite stall before broad scheduler changes. Registered host callbacks and missing-function policy must not be swallowed by game-specific non-code-call filters.

## Gameplay QA

- Validate process-local keyboard/controller input through the title, New Game, loading, and first level.
- Confirm Wolverine movement, camera tracking, HUD updates, enemy behavior, music, ambient police sirens, and stable frame progression over a sustained session.
- Confirm the user can close the runtime normally and that every automated probe closes its own process and restores the retail archive.
- Retain global white-wireframe mode as a diagnostic for 3D world and character geometry; normal textured rendering stays the default.

## Project Hygiene

- Post-Probe-2276 cleanup removed 185 generated Debug directories (about 6 MiB) and 24 disposable files (about 15 MiB), including current probe images and the temporary timing baseline. The workspace is 7.691 GiB / 29,038 files including the ISO. All eight protected artifacts, 19 active generated block sources plus their dispatcher, both private captures, and the original/staged executable hashes were verified. The cleanup source-file rule now uses the same public-plus-private block limit as directory cleanup. No owned runtime, observer, test, or build process remains.
- The initial September 4 native-block integration links exceeded the 2048 MiB cap. On September 5, the complete runner-only non-IPO rebuild and link succeeded under that same cap. Build-helper regression tests pass in ordinary and unity modes, including resource-limited reconfiguration and primary preservation. The gameplay observer's syntax, timing endpoints, duplicate suppression, missing-framebuffer rejection, and complete/incomplete PE checks pass; full live validation remains pending. Cleanup preserves named primary/staged/candidate runtimes regardless of age.
- Post-integration cleanup removed 183 generated Debug directories (about 6 MiB). A complete workspace inventory including the original ISO and hidden files is 8.430 GiB / 28,901 files: about 4.30 GiB in the active build, 2.00 GiB in the game working directory, and 2.02 GiB in the original ISO. No new gameplay images or per-probe executable backups were created. All eight policy-protected artifacts remain; both saved gameplay hashes were reverified. Owned build/test/runtime processes are closed.
- September 5 build checkpoint: the guarded cleanup found no stale files to remove. The complete workspace inventory was 7.664 GiB / 28,923 files, including the original ISO and fixed-slot candidate. All eight policy-protected artifacts remained, the primary/staged hashes were unchanged, and all owned build/test/runtime processes were closed before the subsequent controlled probes.
- September 5 post-Probe-2272 cleanup counts public synthetic blocks separately from the configured private-block limit, preventing active generated block directories from being misclassified as stale. It removed 184 Debug directories (about 6 MiB) and 43 generated files (about 10 MiB). The complete workspace inventory is 7.674 GiB / 28,964 files; generated block 17, the latest candidate, and all eight policy-protected artifacts remain. All owned build/test/runtime processes are closed.
- Staged `ps2EntryRunner.next.exe` remains the Probe 2264 ordinary build, SHA-256 `6594A68623BA1E16557CA002DFF5FA2C29A397CEBE2D4A4E053E4C9ED9E42877`. The primary remains unchanged at `90D374181A33AFB9E872E44E7D5140E0685E4787AB654D33AB18EF5D56220105`. The live-validated native-block candidate is `ps2EntryRunner.candidate.exe`, SHA-256 `42FFBDA55226B7A55AE07311A9BC35049165ADC6EA0DF46636B53BB49020108C`; detailed VU profiling is compiled out. Ordinary launchers still select the staged build and clear experimental environment variables.
- The last accepted ordinary candidate remains staged as `ps2EntryRunner.next.exe`, which existing staged-runtime launchers prefer. The primary remains the fallback; the newer native candidate and diagnostics remain opt-in.
- `build-below-normal.ps1 -CompileOnly` forces recompilation of the selected runner target without linking. Build its dependencies first, then use `-LinkOnly -OutputName <candidate>` to preserve the primary. Ordinary and unity-build fixtures verify new runner behavior, retained library contents, unchanged executables before linking, and expected compile failures; both pass and return success correctly.
- The stale `ps2EntryRunner.next.exe.exe` still needs removal once executable cleanup is permitted. A prior cleanup command was rejected by tool policy; do not retry that deletion through a workaround.
- Removal of the GS unit-test output `PS2Recomp/gs-present-first-nonblack-0.ppm` was also policy-blocked. The guarded runner now explicitly preserves this root-level file; do not retry its removal through another mechanism. This is synthetic test output, not gameplay evidence.
- Cleanup of the temporary `ps2xTest/Release/ps2x_tests.vu-baseline.exe` and `ps2x_tests.vu-retire.exe` was also rejected by tool policy on September 4. These are about 10.4 MiB combined, not active runtime candidates. Leave them pending rather than retrying through another mechanism.
- September 4 checkpoint after Probe 2261: workspace is 6.039 GiB across 27,568 files. Guarded artifact cleanup ran again. All owned runtime, observer, test, and build processes are closed.
- September 4 checkpoint after Probe 2263: cleanup removed three old image artifacts (2.7 MiB); workspace is 6.050 GiB across 27,602 files, including the two policy-blocked benchmark executables. All owned runtime, observer, test, and build processes are closed.
- September 4 checkpoint after Probe 2264: cleanup removed three old artifacts (2 MiB); workspace is 6.060 GiB across 27,632 files. This includes one fixed-slot 10 MB test linker map and the existing policy-blocked benchmark executables; no additional executable backup slots were created.
- September 4 post-experiment checkpoint: guarded cleanup ran again; workspace remains 6.060 GiB across 27,673 files at measurement. No screenshots or gameplay probes were taken this turn, and all three saved runtime executable hashes above were reverified unchanged. The existing benchmark slot was reused, and temporary build-test directories were removed.
- Fixed the 12-hour generated-image cleanup scope to include `xmen-legends` and its `disc` directory, not just inspection/build folders. A controlled old/fresh PPM fixture check proves the expired image is removed and the fresh one survives; both fixtures were cleaned up. Tracked README screenshots remain protected.
- Run the integrated probe cleanup, retain only useful diagnostic milestones, remove generated images older than 12 hours, and verify repository size.
- Do not modify or revert the user's dirty `dev-overrides/intro_normal.gameplay-map.py` or `inspect-igb-scene.py` files.
- Keep all compiler, recompiler, CMake, and MSBuild work BelowNormal, affinity `0xF`, one worker, and headless.
- `build-below-normal.ps1` now checks each owned compiler/linker's private memory, with a default 2048 MiB limit. Exceeding it terminates the owned build job. This guard was exercised by an oversized native-kernel build; the earlier monolithic forced-inlining experiment was stopped manually after exceeding 14 GiB. Native kernels now use template instruction selection and normal inlining in separate eight-kernel build targets. Do not restore forced inlining of the whole arithmetic call graph.
- September 4 native-prototype cleanup: guarded cleanup completed; workspace inventory is 28,118 files / 6.060 GiB. Explicit removal of obsolete `out/xmen-final3-build/ps2xRuntime/vu_native_words.inc` and `ps2_vu_native_upper.dir/Release/vu_native_batch_152.obj` was policy-blocked; leave these 65,071 bytes in place rather than retry through another mechanism. No new screenshots or game processes were created, and all three saved runtime executable hashes remain unchanged.
- The first native-enabled full runner compile was stopped by the 2048 MiB watchdog. `-CompileOnly` now obtains evaluated source/PCH metadata from MSBuild and launches a fresh compiler for each active translation unit, PCH first; unity originals are excluded. Both ordinary and unity fixtures pass, including changed code in two runner sources and an intentional compilation failure that leaves both executables unchanged. The memory limit was not raised; the later runner-only non-IPO rebuild completed all 107 units and linked the fixed candidate slot successfully.
- Native ABI fingerprints now live in a generated configuration-specific header. Arithmetic-only fingerprint changes should rebuild ABI consumers rather than force every generated guest source to compile again. Class-layout/header changes still require a consistent runtime/runner rebuild.
- Later tooling simplification: the installed MSBuild targets expose `UseMultiToolTask`, `MultiProcMaxCount`, and `EnableClServerMode`. Test their per-file behavior with one process and server mode disabled before replacing the verified explicit-source loop. Do not interrupt this rebuild or raise the memory limit to experiment with them.
- Added opt-in `PS2X_RUN_VSYNC_LIMIT` for probes. A nonzero unsigned 32-bit decimal tick requests a normal stop and joins the game thread; zero/absence disables it. Use a later guard timeout as a fallback, leaving normal user-driven window closing intact. Runner native counters are read only after `run()` returns.
- September 4 post-native-game checkpoint: guarded cleanup removed three old artifacts (2.4 MiB); workspace inventory is 28,491 files / 6.060 GiB. Both runtime processes, observers, and the build are closed. Two final gameplay captures were retained; no desktop/window captures or host input were used. The policy-blocked root GS test image remains untouched.

## Delivery

- Update the public repository README with setup requirements, legal asset expectations, build/run commands, controls, current limitations, and screenshots including first gameplay.
- Commit and push accepted OpenXML1 changes.
- Keep PS2Recomp changes general and focused; submit suitable fixes through the public fork and upstream pull requests.
- Recheck current upstream PS2Recomp pull requests before the next integration pass.
