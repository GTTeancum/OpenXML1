# X-Men Legends PS2Recomp TODO

Work resumed on 2026-09-04 at the user's request.

## Resume Point

- Keep SFD playback out of scope. The target remains basic first-level playability.
- Preserve the known-good primary runtime at `PS2Recomp/out/xmen-final3-build/ps2xRuntime/Release/ps2EntryRunner.exe`.
- The environment-cache change retains the original dispatch gate and checkpoint batching. The unconditional early-dispatch experiment was removed. Current candidate runs reach rendered gameplay; no standalone frame-rate gain is claimed for caching.
- Probe 2251 re-established textured first-level rendering with the known-good primary runtime, `TitleGameplayFirst`, host clock, fast legal handling, aggressive branch-hook bypass, and full CPU rasterization. `probe2251-present1099.png` is the current gameplay evidence.
- User testing confirmed that Wolverine can move and basic combat works in the first level, although gameplay is currently extremely slow.
- Do not use `PS2X_SKIP_CPU_RASTER_BEFORE_PRESENT` for correctness or visual validation with host-clock pacing. Probe 2248 reached the NYC package quickly but later entered an invalid guest state and rendered black.
- Previous selected-source performance conclusions are invalidated: the helper compiled objects without rebuilding `ps2_runtime.lib`, so candidate executables could still contain old code. Do not conclude that environment caching failed or checkpoint batching caused a fault from those runs.
- Fixed the helper to run `_Lib` without `SelectedFiles` after a successful static-library source compile. Ordinary and unity-build regression tests prove candidate behavior changes, untouched library code remains available, and the primary executable is preserved.
- Probe 2252 reproduced full-rendering first-level progression to present 1140 with the unchanged primary. Across presents 1000-1140, CPU rasterization averaged 94.57 ms (44.60 ms triangle lists, 46.35 ms strips), about 23,424 submissions per frame. This is raster time, not total frame time.
- Probe 2253 tested the verified rebuilt candidate with the usual bypass enabled. Its present-1140 native capture confirms textured New York and Wolverine with the existing black props/HUD defects. The corrupted image at present 768 was still loading, not a permanent world-rendering regression. First batches over 20,000 submissions occurred at present 964 in Probe 2252 and 1010 in Probe 2253; do not compare an arbitrary earlier present as if loading had finished.
- Probe 2254 tested the broader early dispatcher with per-branch checkpoints, reached present 1140, and closed normally under the guard. This experiment was not retained. Concurrent test/build work makes its elapsed time unsuitable for a clean performance comparison. No practical frame-rate improvement is established.
- Focused tests after restoring the original dispatch control flow: VU 91/91; GS 76/79; dispatch 2/5. The three dispatch failures reproduce with and without the early-path change: indirect-call registration/return and missing-target tests encounter existing non-code-call filters. The three GS failures concern CSR/IMR access and two T4 triangle sampling cases. The full suite was interrupted; do not report a full-suite pass.
- Probes 2255/2256 compare early depth rejection off/on in the same executable. Across presents 1100-1400, mean CPU raster time fell from 98.98 ms to 63.03 ms (36%). This is not total frame time. A 20-second gameplay sample with the GS profiler enabled still measured only about 2.7 FPS.
- Probe 2257 replayed all 23,042 triangle batches in gameplay present 1200 against private copies of their starting VRAM and CLUT, comparing all 4 MiB after every draw: zero mismatches. Earlier separate-run screenshots differ in dynamic effects/HUD state; they are not deterministic frame comparisons.
- Early-depth synthetic tests compare all VRAM bytes across 128 cases: identical on/off digest `e5a1e2f16d5d1336` in the bring-up branch. Corrected two T4 fixtures to request CLUT loading (CLD=1), without changing expected pixels. Current local GS suite: 80/81, with only the existing CSR access failure. VU: 91/91. Profiling helper: 2/2. The broader local kernel suite did not finish and was stopped; it is not a passing result.
- Submitted focused upstream PR #246, `https://github.com/ran-j/PS2Recomp/pull/246`, from the existing clean checkout. Upstream-main-based Release suite: 427/427. Both complete-suite runs match digest `e1af4f1cfc948cc3`; the digest differs from the bring-up branch because the rendering implementations differ.

## Performance And Correctness

- Prioritize VU execution cost. Probe 2258's 80 complete timing windows between gameplay presents 1101 and 1299 cover 88,161 ms: VU exclusive 65,177 ms (73.93%), GS submission/drawing 16,195 ms (18.37%), other guest execution 3,181 ms (3.61%), transfers excluding VU/GS 2,801 ms (3.18%), scheduler 218 ms, events 433 ms, and waits 0.1 ms. These are opt-in instrumented wall-time measurements, not a new frame-rate claim.
- Probe 2259 completed the sampled VU diagnostic through present 1200. Its 39 gameplay timing windows cover 44,364 ms. Among named sampled instruction stages, upper arithmetic and pipeline retirement are the largest. Do not treat the remaining outer-slice time as pure interpreter work: it includes instrumentation overhead, which is material for these very short helpers.
- Detailed VU instrumentation now requires the CMake option `PS2X_ENABLE_VU_DETAIL_PROFILE=ON` plus runtime variables `PS2X_RUNTIME_PHASE_PROFILE=1` and `PS2X_VU_DETAIL_PROFILE=1`. It samples 1/64 complete VU slices after tick 1000. Keep the CMake option OFF in ordinary gameplay builds so per-instruction profiling branches are absent.
- Operand-normalization inlining and an upper-NOP fast return passed all 93 local VU tests. Nine arithmetic-workload runs before/after retain digest `45e6d3166964d75e`; median execution time is 21.103 ms before and 19.971 ms after on this machine (about 5%). This is a synthetic workload, not a gameplay FPS measurement.
- Probe 2260 reached present 1200 and its native framebuffer confirms textured New York/Wolverine with the existing black-prop, HUD, and red-circle defects. It closed under the guard and restored the startup archive. Comparable complete diagnostic windows averaged 441.09 ms/frame before (2259, 98 frames) and 382.26 ms/frame after (2260, 96 frames), still about 2-3 FPS. These shared-machine, instrumented runs are not an isolated or sustained frame-rate guarantee.
- Submitted upstream PR #247, `https://github.com/ran-j/PS2Recomp/pull/247`, from commit `cf1efac` in the existing clean checkout. Both upstream-main-based test runs pass 427/427 with arithmetic digest `3308d7380e3bfe64`. The runtime change has no dependency on the occupancy-mask PR or X-Men-specific code. Bring-up commit `8cbe200` is pushed separately.
- The ordinary build with detailed profiling compiled out also passes 93/93 VU tests and preserves digest `45e6d3166964d75e`. Probe 2261 reached present 1280 with normal textured first-level output and existing lighting/HUD/effect defects. Neither phase nor raster profiler was enabled. External observation of native present-log entries 1152 to 1280 measured 128 frames in 46.576 seconds, approximately 2.748 FPS. This does not establish a meaningful overall improvement over the earlier roughly 2.7 FPS sample.
- Probe 2261's guard closed the runtime and restored the startup archive. Its tested ordinary binary is staged as `ps2EntryRunner.next.exe`; `probe2261-present1280.png` is the current native framebuffer checkpoint. The task remains active and practical interactive speed is not achieved.
- Probe 2258 reached present 1300, its native framebuffer shows textured New York and Wolverine with known black-prop/HUD/effect defects, and the guard closed the runtime and restored the startup archive. `probe2258-present1300.png` is the current checkpoint capture.
- Use `PS2X_RUNTIME_PHASE_PROFILE=1` for phase timings (`calls/inclusive-ms/exclusive-ms` per stage), `PS2X_GS_CPU_PROFILE=1` for raster-only timings, and `PS2X_GS_VERIFY_EARLY_DEPTH_PRESENT=<present>` for one-frame same-state triangle verification. Leave these disabled for ordinary play. The phase profiler reports only completed outer scopes and subtracts nested work.
- Added bounded process-local VU snapshot/replay in bring-up commit `eaee632`. Probe 2262 captured 32 real slices in 1,914,088 bytes; 4096 timed executions reproduce every register/pipeline field, all VU data bytes, and emitted GIF bytes/cycles, digest `75d4ff1e67bbbc4c`. See `VU-REPLAY.md`; the capture remains private in ignored `disc/vu-replay.bin`.
- Removed redundant same-cycle pipeline retirement scans and optimized contiguous PATH1 qword copying. Local VU tests pass 97/97, including boundary and zero/one-cycle resume checks. Retirement-only replay medians are 589.740 ms before and 564.147 ms after over nine interleaved pairs (about 4.3%). Packet-copy incremental timings were noisy; no independent gain or whole-game FPS improvement is claimed.
- Submitted upstream PR #248, `https://github.com/ran-j/PS2Recomp/pull/248`, commit `ebd1d34`. Clean upstream main passes 425/425 tests and the PR passes 426/426. It does not depend on earlier VU PRs or the replay diagnostics.
- Probe 2263 reached present 1280 with textured New York and Wolverine and the known black-prop, missing-foliage, and HUD defects. Native capture: `probe2263-present1280.png`. Replay and both performance profilers were off; detailed profiling was compiled out. External log observation measured 128 frames from 1152 to 1280 in 41.0026 seconds, or 3.1218 FPS. This is better than the earlier 2.748 FPS sample, but not an isolated or sustained-speed guarantee. The guard closed the runtime and restored the startup archive; the observer is also closed.
- Implemented sparse VI-mask iteration in bring-up commit `746e884` and PR #248 follow-up `a64d924`. All 256 pairs of VI source registers cover VI0, high registers, every destination, repeated loads, final values, stalls, and the end-bit delay slot. Local VU tests pass 98/98; the clean upstream PR passes 427/427.
- The longer VI-mask replay comparison alternates execution order across nine pairs with 512 repeats per case. Median time falls from 2299.731 ms to 1981.324 ms (13.85%), with unchanged digest `75d4ff1e67bbbc4c` and every candidate run faster than every baseline run in that batch. Ordinary Probe 2264 reaches present 1280 with textured New York/Wolverine and existing visual defects, measuring 3.2485 FPS over 128 frames in 39.4022 seconds. This is not an isolated or sustained-speed guarantee. Runtime and observer are closed; the startup archive is restored.
- Added an opt-in, offline-only native sampler to the replay test, plus a build-identity-checked MSVC-map report. Confirmation: 895 samples, no drops/failures, 65,536 timed replay slices with identical results. Among the 776 in-module samples, retirement is 22.29%, VF write queuing 9.28%, FMAC flag queuing 8.63%, and the main VU loop 12.76%. Sampling includes replay restoration/comparison and must not be used as an ordinary FPS benchmark. The sampler touches only its own test thread and always resumes before allocating/logging.
- Rejected a completion-cycle index for pending flags/VF writes on September 4. It passed all 100 focused VU tests and exact real-game replay, but nine 512-repeat pairs had medians 2148.903 ms baseline / 2131.059 ms candidate, while seven longer 2048-repeat pairs had medians 7873.528 / 7956.781 ms. The inconsistent timing does not justify the added derived state. The index and replay reconstruction changes were removed; slot-reuse, single-cycle, and restart/reset regression tests are retained. No gameplay speed gain is claimed from this experiment.
- Rejected returning cached decoded instruction pairs by reference. All 119 VU-related tests and the real-game replay passed, but nine alternating 1024-repeat pairs had medians 3923.547 ms baseline / 4080.658 ms candidate (about 4% slower). The runtime change was removed; the new tracked/untracked, misaligned-PC, nullable-memory, and repeated-code-edit regression is retained. No new game probe or runtime replacement is justified by either rejected experiment.
- Restored production code passes 101/101 focused vector tests and 119/119 VU-related tests; 4096 real replay executions retain digest `75d4ff1e67bbbc4c`. Bring-up regression commits `15dbbda` and `d65a574` are pushed. PR #245 now includes the slot-reuse and cancellation tests in `af56130`, with 427/427 clean upstream-based Release tests. The existing clean checkout is on `codex/vu-pipeline-occupancy-masks`; no new clone was created. The PR body no longer treats an earlier small title-screen timing sample as a current gameplay-speed result.
- Next major performance task: prototype compiled VU execution in offline replay, with an interpreter fallback. Preserve exact cycle-budget returns, pending arithmetic behavior, code-generation invalidation, and PATH1 bytes/emission cycles before integration. Inspect the existing PCSX2 microVU/emitter interfaces for reuse, but do not import its block overshoot or global emulator dependencies unchanged. If additional clean-entry captures are required, add them without weakening the existing mid-slice oracle. Move beyond per-instruction bookkeeping micro-optimizations.
- Native-upper preparation: the shared-arithmetic extraction passes all 119 VU-related tests. A one-cycle cold replay identifies 105 upper-instruction candidates while preserving all 32 captured results and digest `75d4ff1e67bbbc4c`. Candidate fetch counts include dependency-stall retries; the generated recipe stays in ignored `disc/`.
- The opt-in Windows native-upper prototype now passes 122/122 VU-related tests, including 58 synthetic instruction kernels across 16 boundary-value patterns, ABI rejection/reload, provider switching, and fallback/cache invalidation. All 32 retail captures match exactly over 4096 timed slices, with 100% native upper-instruction coverage. The private DLL is 86 KiB. It is loaded only by tests, not by the game.
- Nine alternating native/interpreted comparisons at 1024 repeats retain digest `75d4ff1e67bbbc4c`: medians 4476.139 ms interpreted / 4164.376 ms native (6.965% less execution time). Native wins every pair, but this is a modest warm-replay gain, not a gameplay FPS result. Keep the prototype experimental. Next: specialize the still-generic exact-result/product-sticky helpers without forced-inlining blowup, then address compiled-block scheduling/retirement. Do not spend another boot probe merely to measure this small initial kernel gain.
- The current local test build is configured with `PS2X_ENABLE_VU_NATIVE_UPPER=ON`; the option defaults OFF in source. Native-enabled class layout differs. Do not relink a game executable from stale runner objects: reconfigure the intended mode and rebuild both runtime dependencies and runner translation units together before any candidate game link.
- Native prototype checkpoint `40a2193` is pushed to the PS2Recomp bring-up branch. The ordinary and unity build-helper fixtures both pass with the memory watchdog present. Native-enabled GS tests remain 80/81 with the same pre-existing CSR read/write failure; no full-suite or gameplay-speed improvement is claimed.
- Native flag specialization now passes 122/122 VU-related tests with 5536 synthetic arithmetic cases (173 words, 32 boundary/random patterns), including masks, in-place destinations, and VF0. The 32 retail captures still match exactly over 4096 timed slices, digest `75d4ff1e67bbbc4c`. Nine alternating 1024-repeat pairs have medians 5232.074 ms interpreted / 4632.082 ms native (11.468% less replay execution time), with native faster in each pair. Raw times across different batches are not an isolated before/after comparison.
- Completed the opt-in native loader validation in a separately linked game candidate, measuring the same executable with and without the provider. The primary and staged Probe 2264 executable are preserved. The loader is shared with the tests and rejects incompatible/relative module paths; unknown instruction words retain the interpreter fallback.
- Experimental runtime checkpoint `cbadbb1` is pushed. After loader integration, VU tests pass 122/122, the focused PS2GS suite remains 80/81 with the existing CSR failure, and 4096 native retail replays retain the exact digest. The consistent runner rebuild and live game validation are recorded below.
- The subsequent 107-step rebuild and separate profile-candidate link succeeded. Probes 2265/2266 both reached textured New York/Wolverine and exited normally at vsync 1400 with the startup scripts restored. Same-executable, profiler-free external observation of presents 1152-1280 measured native 36.7985431 seconds / 3.4784 FPS and interpreted 38.0110288 seconds / 3.3674 FPS. This small difference between two sequential shared-machine runs does not establish a meaningful sustained gain. Both native framebuffers retain black-prop, missing-foliage, HUD, and effect defects; these are not deterministic pixel comparisons.
- Native-run post-join VU1 counters are 1,550,972,856 native / 233,407,006 interpreted (86.92% native over the whole run); the interpreted run reports 0 / 1,290,629,286. VU0 remains 0/0. The game loader's negative missing/relative-module test exits 1 before window initialization. Keep the candidate experimental; do not replace the staged build for this small measured difference.
- Next performance work: prototype compiled instruction blocks in offline replay to eliminate more per-pair decode/dispatch and pipeline bookkeeping. Keep the interpreter fallback and prove exact 0/1/64-cycle resume boundaries, upper/lower operand hazards, delayed writes/flags, branch/end-bit behavior, microcode invalidation, and PATH1 payload/emission cycles. The current upper-only kernels leave the principal scheduling/retirement path interpreted. Do not resume tiny full-boot timing probes or visual polish while gameplay remains this slow.
- Added a bounded private pair-recipe exporter as the first compiled-block input. Its one-cycle cold pass counts only retired PCs, excluding dependency-stall retries, and writes exact PC/lower/upper triples plus replay entries and observed successors without state, memory, or GIF payloads. All 32 gameplay captures use the same 16 KiB code image and exercise 16 entries, 426 PCs, 439 edges (26 non-linear), 199 unique lower words, and 105 unique upper words. Focused PS2VU1 is 54/54; the retail replay remains 32/32, 40,803 cycles at one measured repeat, digest `75d4ff1e67bbbc4c`.
- The exporter now partitions straight-line coverage at entries, control-flow/missing edges, and a maximum eight-pair compiler unit: 80 blocks averaging 5.32 pairs, maximum eight. The ranked-word records expand the ignored manifest to 56,202 bytes. Duplicate PCs, invalid successors, unaligned/out-of-range addresses, zero counts, and unbounded limits are rejected.
- The opt-in exact-pair AOT prototype compiles 64 private pairs plus three public synthetic pairs. PS2VU1 passes 55/55; pair mode off/on both reproduce all 32 retail captures, 40,803 cycles at one repeat, and digest `75d4ff1e67bbbc4c`. The top-64 set handles 62,982/80,194 retired pairs (78.54%).
- The direct whole-block prototype now compiles 16 private replay-ranked blocks plus one public synthetic block. It handles 49,182/80,194 retired pairs (61.33%) inside 2,682 successful block entries, and all 32 captures retain exact state, VU memory, GIF output, 40,803 cycles, and digest `75d4ff1e67bbbc4c`. Seven alternating 1024-repeat comparisons measured 4,361.182 ms for exact-pair mode and 3,795.386 ms with blocks at the median, a 12.973% VU replay reduction. Public PS2VU1 is 53/53; replay-enabled PS2VU1 is 54/54. This is not yet a gameplay FPS result.
- A 32-private-block trial raised coverage to 53,170/80,194 pairs (66.30%) but was 9.836% slower than 16 blocks in a direct seven-round alternating comparison (3,580.251 vs 3,259.639 ms median). Keep the active private limit at 16; the larger generated set and comparison binary were removed.
- Nine alternating 128-repeat comparisons measure interpreter median/mean 501.040/501.156 ms and native-pair 465.445/468.259 ms, a 7.10% median reduction with the native path faster in every pair. This is exact offline replay, not a gameplay FPS result. PS2Recomp checkpoint `f14bdea` is pushed.
- Native-pair sampling places `commitReadyPipelines` at 25.20% of in-module samples, `run` at 17.07%, `queueVfWrite` at 10.16%, and `updateFmacFlags` at 8.54%. Specialized instruction bodies are no longer the main sampled cost. Next implementation step: reduce queue/retirement crossings with exact compiled scheduling while preserving mid-slice pending state, code invalidation, branches, cycle budgets, and PATH1 timing. Do not spend a full gameplay probe on the current 7.10% replay-only gain.
- Inspected PCSX2 microVU at local reference commit `d073d7501` (August 30). It precomputes pipeline timing while compiling blocks, but depends on global VU/EE state, GIF/VIF scheduling, MTVU, configuration, executable-memory management, and its emitter. It is not a drop-in library. A future compiled path must preserve this runtime's slice boundaries and PATH1 visibility rather than import PCSX2's block-level cycle accounting unchanged.
- Small interpreter savings alone are insufficient for practical playability. Use the replay to quantify larger execution-path changes, and evaluate a compiled VU path if interpreter improvements plateau. Do not turn this into an unbounded series of full-boot micro-optimization probes.
- Preserve floating-point edge cases, flags, pipeline timing, XGKICK order, and self-modifying microcode behavior while optimizing VU execution. The existing occupancy-mask optimization is already integrated.
- Measure dispatch/checkpoint costs before changing scheduler policy, but the new broad profile does not identify dispatch or waiting as the dominant gameplay cost.
- Investigate missing recompiled target `0x00504310`, reached by an indirect call at `0x001BD21C`. Probe 2251 continued and rendered, but this remains a correctness gap.
- Profile the runtime during actual first-level rendering, without thread suspension during correctness runs.
- Optimize the measured CPU triangle raster cost; preserve a full-rendering baseline for comparison.
- Improve sustained frame rate from the current single-digit range to practical interactive speed.
- Resolve the remaining CSR/IMR and three dispatch-test failures, and identify the local kernel-suite stall before broad scheduler changes. Registered host callbacks and missing-function policy must not be swallowed by game-specific non-code-call filters.

## Gameplay QA

- Validate process-local keyboard/controller input through the title, New Game, loading, and first level.
- Confirm Wolverine movement, camera tracking, HUD updates, enemy behavior, music, ambient police sirens, and stable frame progression over a sustained session.
- Confirm the user can close the runtime normally and that every automated probe closes its own process and restores the retail archive.
- Retain global white-wireframe mode as a diagnostic for 3D world and character geometry; normal textured rendering stays the default.

## Project Hygiene

- Staged `ps2EntryRunner.next.exe` remains the Probe 2264 ordinary build, SHA-256 `6594A68623BA1E16557CA002DFF5FA2C29A397CEBE2D4A4E053E4C9ED9E42877`. The primary remains unchanged at `90D374181A33AFB9E872E44E7D5140E0685E4787AB654D33AB18EF5D56220105`. The separate native-enabled `ps2EntryRunner.profile.exe` candidate is now `EA710AE476878943CD20C355A875A15AD7BB2D2EA426195C5975FEAB01CB01B7`, with matching DLL `1B7AFB2D0D4309F15BB63FBD893411A123A8029E4042787613748CD519B74FF8`; detailed VU profiling is compiled out. Ordinary launchers still select the staged build and clear experimental environment variables.
- The last accepted ordinary candidate remains staged as `ps2EntryRunner.next.exe`, which existing staged-runtime launchers prefer. The primary remains the fallback; the newer native candidate and diagnostics remain opt-in.
- `build-below-normal.ps1 -CompileOnly` forces recompilation of the selected runner target without linking. Build its dependencies first, then use `-LinkOnly -OutputName <candidate>` to preserve the primary. Ordinary and unity-build fixtures verify new runner behavior, retained library contents, unchanged executables before linking, and expected compile failures; both pass and return success correctly.
- The stale `ps2EntryRunner.next.exe.exe` still needs removal once executable cleanup is permitted. A prior cleanup command was rejected by tool policy; do not retry that deletion through a workaround.
- Removal of the GS unit-test output `PS2Recomp/gs-present-first-nonblack-0.ppm` was also policy-blocked. The guarded runner now explicitly preserves this root-level file; do not retry its removal through another mechanism. This is synthetic test output, not gameplay evidence.
- Cleanup of the temporary `ps2xTest/Release/ps2x_tests.vu-baseline.exe` and `ps2x_tests.vu-retire.exe` was also rejected by tool policy on September 4. These are about 10.4 MiB combined, not active runtime candidates. Leave them pending rather than retrying through another mechanism.
- September 4 checkpoint after Probe 2261: workspace is 6.039 GiB across 27,568 files. Guarded artifact cleanup ran again. All owned runtime, observer, test, and build processes are closed.
- September 4 checkpoint after Probe 2263: cleanup removed three old image artifacts (2.7 MiB); workspace is 6.050 GiB across 27,602 files, including the two policy-blocked benchmark executables. All owned runtime, observer, test, and build processes are closed.
- September 4 checkpoint after Probe 2264: cleanup removed three old artifacts (2 MiB); workspace is 6.060 GiB across 27,632 files. This includes one fixed-slot 10 MB test linker map and the existing policy-blocked benchmark executables; no additional executable backup slots were created.
- September 4 post-experiment checkpoint: guarded cleanup ran again; workspace remains 6.060 GiB across 27,673 files at measurement. No screenshots or gameplay probes were taken this turn, and all three saved runtime executable hashes above were reverified unchanged. The existing benchmark slot was reused, and temporary build-test directories were removed.
- Fixed the 12-hour generated-image cleanup scope to include `xmen-legends` and its `disc` directory, not just inspection/build folders. A controlled old/fresh PPM fixture check proves the expired image is removed and the fresh one survives; both fixtures were cleaned up. Tracked README screenshots remain protected.
- Run the integrated probe cleanup, retain only useful diagnostic milestones, remove generated images older than 12 hours, and verify repository size.
- Do not modify or revert the user's dirty `dev-overrides/intro_normal.gameplay-map.py` or `inspect-igb-scene.py` files.
- Keep all compiler, recompiler, CMake, and MSBuild work BelowNormal, affinity `0xF`, one worker, and headless.
- `build-below-normal.ps1` now checks each owned compiler/linker's private memory, with a default 2048 MiB limit. Exceeding it terminates the owned build job. This guard was exercised by an oversized native-kernel build; the earlier monolithic forced-inlining experiment was stopped manually after exceeding 14 GiB. Native kernels now use template instruction selection and normal inlining in separate eight-kernel build targets. Do not restore forced inlining of the whole arithmetic call graph.
- September 4 native-prototype cleanup: guarded cleanup completed; workspace inventory is 28,118 files / 6.060 GiB. Explicit removal of obsolete `out/xmen-final3-build/ps2xRuntime/vu_native_words.inc` and `ps2_vu_native_upper.dir/Release/vu_native_batch_152.obj` was policy-blocked; leave these 65,071 bytes in place rather than retry through another mechanism. No new screenshots or game processes were created, and all three saved runtime executable hashes remain unchanged.
- The first native-enabled full runner compile was stopped by the 2048 MiB watchdog. `-CompileOnly` now obtains evaluated source/PCH metadata from MSBuild and launches a fresh compiler for each active translation unit, PCH first; unity originals are excluded. Both ordinary and unity fixtures pass, including changed code in two runner sources and an intentional compilation failure that leaves both executables unchanged. The memory limit has not been raised. A fresh 107-step runner compile is required before linking; do not use the incomplete objects from the aborted build as a candidate.
- Native ABI fingerprints now live in a generated configuration-specific header. Arithmetic-only fingerprint changes should rebuild ABI consumers rather than force every generated guest source to compile again. Class-layout/header changes still require a consistent runtime/runner rebuild.
- Later tooling simplification: the installed MSBuild targets expose `UseMultiToolTask`, `MultiProcMaxCount`, and `EnableClServerMode`. Test their per-file behavior with one process and server mode disabled before replacing the verified explicit-source loop. Do not interrupt this rebuild or raise the memory limit to experiment with them.
- Added opt-in `PS2X_RUN_VSYNC_LIMIT` for probes. A nonzero unsigned 32-bit decimal tick requests a normal stop and joins the game thread; zero/absence disables it. Use a later guard timeout as a fallback, leaving normal user-driven window closing intact. Runner native counters are read only after `run()` returns.
- September 4 post-native-game checkpoint: guarded cleanup removed three old artifacts (2.4 MiB); workspace inventory is 28,491 files / 6.060 GiB. Both runtime processes, observers, and the build are closed. Two final gameplay captures were retained; no desktop/window captures or host input were used. The policy-blocked root GS test image remains untouched.

## Delivery

- Update the public repository README with setup requirements, legal asset expectations, build/run commands, controls, current limitations, and screenshots including first gameplay.
- Commit and push accepted OpenXML1 changes.
- Keep PS2Recomp changes general and focused; submit suitable fixes through the public fork and upstream pull requests.
- Recheck current upstream PS2Recomp pull requests before the next integration pass.
